# harness-flow-progress

Cycle 1 (2026-05-04 04:30) — [P0] phantom CLAUDE.md citations in 8 skill Authoritative source lines — queue: 14 remaining — PW: n/a
Cycle 2 (2026-05-04 04:45) — [P0/P1/P2] 20 issues resolved: trigger keywords, override caveats, doc-only glob, branch placeholder, §27 added, JSONL parser, npm placeholder, synthesized annotations — queue: 0 remaining — PW: n/a
Cycle 3 (2026-05-04 05:10) — [P0/P1/P2] 17 issues resolved: 12시간 trigger restored, §27 in CLAUDE.md, deepinit ToolSearch fix, anchor fixes, plugin-halt removed, auto-install restricted — queue: 0 remaining — PW: n/a
Cycle 4 (2026-05-04 06:00) — [P0/P1] ~20 issues resolved: Gate 1.5 added to harness-share.md §3, Task()→Agent() in §22, rollback policy in §2, pre-PR deepinit timing (autonomous-boundary + loop), coverage cross-link in large-task-delegation, "In autonomous mode" section in pre-merge-sync, codex fallback file-write mandate strengthened, PR footer 5-gate→6-gate, CLAUDE.md Gate 1.5 triggers, code-review-graph external tools section — version bumps: pre-commit-gate 1.0.4, web-loop 1.3.3, codebase-survey 1.2.2, large-task-delegation 1.0.4, autonomous-boundary 1.0.4, autonomous-loop 1.0.3, pre-merge-sync 1.0.3, codex-cross-verification 1.0.4, test-coverage 1.0.2 — queue: 0 remaining — PW: n/a
Cycle 5 (2026-05-04 06:45) — [P0/P1/P2] 21 issues resolved: gate-count drift harmonized across pre-commit-gate/autonomous-boundary/web-loop/README (5→6, add Gate 1.5), git merge-base→git diff --name-only in autonomous-loop, reviewer FAIL halt added to pre-commit-gate failure protocol, deepinit_manifest fallback when OMC not installed, codex exit-code probe + error stub, pip--user + PEP 668 fallback in codebase-survey, web-loop moves/copies→copy, PR footer gate count disambiguation (no AGENTS.md case), reviewer FAIL count definition (per-reviewer or 3+ total), feature dir definition in codebase-survey, silent-skip anti-pattern in test-coverage, web-loop branch note — version bumps: pre-commit-gate 1.0.5, autonomous-boundary 1.0.5, web-loop 1.3.4, autonomous-loop 1.0.4, codebase-survey 1.2.3, codex-cross-verification 1.0.5, pre-merge-sync 1.0.4, test-coverage 1.0.3 — queue: 0 remaining — PW: n/a
Cycle 6 (2026-05-04 07:30) — [P0/P1/P2] 13 issues resolved: harness-share.md §25 gate count 5→6, "8 steps"→"all steps (Step 0.5 + Step 1-8)" in codebase-survey/large-task-delegation/web-loop, pre-commit-gate "4 gates"→"all applicable gates", Explore→oh-my-claudecode:explore in large-task-delegation, pre-merge-sync "usually"→explicit phrasing, codex non-parseable stdout fallback, codex fallback overwrite rule, web-loop Q-WEBLOOP prefix convention, pre-commit-gate web-loop override exception cross-ref, test-coverage exit-code anti-pattern — version bumps: codebase-survey 1.2.4, web-loop 1.3.5, large-task-delegation 1.0.5, pre-commit-gate 1.0.6, pre-merge-sync 1.0.5, codex-cross-verification 1.0.6, test-coverage 1.0.4 — queue: 0 remaining — PW: n/a
Cycle 7 (2026-05-04 08:10) — [P0/P1/P2] 8 issues resolved: harness-share.md §3 line 200 "5-gate/4-gate"→"6-gate/5-gate", §26 heading "8-step"→"Step 0.5 + Step 1-8", §3 doc-only glob add skills/**/*.md, pre-commit-gate "6 gates in order"→"up to 6 gates" + Gate 0 conditional note, autonomous-boundary "6-gate PASS"→"full PASS (6 or 5)", README gate note (Gate 0 conditional), pre-commit-gate 3-fail halt: autonomous-only qualifier, codex-cross-verification PIPESTATUS note — version bumps: pre-commit-gate 1.0.7, autonomous-boundary 1.0.6, codex-cross-verification 1.0.7 — queue: 0 remaining — PW: n/a
Cycle 11 (2026-05-04 11:05) — [P0/P1/P2] 15 issues resolved: autonomous-boundary harness-test carve-out removed (CLAUDE.md feature/<topic> is the single rule), playwright-verification web-loop override exception added, pre-merge-sync autonomous deepinit always re-runs before gh pr create, web-loop Step 1a path clarified + fallback 3+FAIL cap added, pre-commit-gate Gate 0 deepinit optional label clarified, codex-cross-verification step-2 stuck-handling references §execution-shape instead of duplicating threshold, codebase-survey Q-INSTALL-CRG only on failure (not pre-attempt), codex authoritative-source line cleaned (impl detail moved to body), tool-retry Q-TOOL template uses kzk-user-queue format, test-coverage coverage command generalized, large-task-delegation deep-interview documented as Skill not Agent, autonomous-loop plan-direction jargon defined, background-monitoring subagent completion verification + session resume restatement added, autonomous-loop silent-resume anti-pattern added — version bumps: autonomous-boundary 1.0.9, playwright-verification 1.0.2, pre-merge-sync 1.0.6, web-loop 1.3.9, pre-commit-gate 1.0.10, codex-cross-verification 1.0.10, codebase-survey 1.2.8, tool-retry 1.0.4, test-coverage 1.0.6, large-task-delegation 1.0.8, autonomous-loop 1.0.6, background-monitoring 1.0.3 — queue: 0 remaining — PW: n/a
Cycle 10 (2026-05-04 10:20) — [P0/P1/P2] 16 issues resolved: harness-share.md §22 omc-ask-codex→codex-exec CLI (align with skill), web-loop plugin pre-flight exit-code capture (silent total-failure path fixed), large-task-delegation explore model haiku→sonnet + deep-interview removed from subagent column (Skill not Agent), pre-commit-gate failure protocol ai-slop-cleaner→critic/verifier/Gate4, pre-commit-gate doc-only AGENTS.md/README.md classification note, pre-commit-gate Gate 0 deepinit_manifest concrete call shape, codebase-survey Step 0.5 build caching guidance, web-loop §4b plan amendment semantics (read-only after Frozen, Frozen v2 for amendments), autonomous-boundary duplicate parenthetical removed, CLAUDE.md production-access triggers synced (SSM removed, added destructive/AKIA/ASIA/aws-vault), codebase-survey dispatch shape pseudocode header, codex prompt skeleton path guard tightened, test-coverage exemption PR description format, tool-retry trigger 'retry' added — version bumps: pre-commit-gate 1.0.9, tool-retry 1.0.3, test-coverage 1.0.5, web-loop 1.3.8, large-task-delegation 1.0.7, autonomous-boundary 1.0.8, codebase-survey 1.2.7, codex-cross-verification 1.0.9 — queue: 0 remaining — PW: n/a
Cycle 9 (2026-05-04 09:35) — [P0/P1/P2] 13 issues resolved: background-monitoring stuck threshold X-minutes placeholder→concrete values (subagent ≥5min, Bash ≥3min, codex 60s first-token/5min total), background-monitoring 30s→60s codex reference, orphan external-project citation removed, harness-share.md §22 "harness-test branch only"→"feature/<topic> branch only", §6 dead ralph-items.md ref→inline Stage 3 description, §3 Gate 0 AGENTS.md/README.md doc-only ambiguity clarified, kzk-tool-retry cross-ref added to autonomous-boundary/web-loop/codebase-survey, large-task-delegation "Gate 0-4"→"Gates 0, 1, 1.5, 2, 3, 4", user-queue "Two sections"→"Three sections", README+CLAUDE.md pip→python3 -m pip --user + pipx fallback, git audit playwright/production-access no bump needed — version bumps: background-monitoring 1.0.2, large-task-delegation 1.0.6, user-queue 1.0.2, autonomous-boundary 1.0.7, codebase-survey 1.2.6, web-loop 1.3.7 — queue: 0 remaining — PW: n/a
Cycle 8 (2026-05-04 08:55) — [P0/P1/P2] 12 issues resolved: codebase-survey Step 1 PATH cache miss fix (consult Step 0.5 CRG_AVAILABLE + add $HOME/.local/bin), web-loop fallback plan path write to docs/plans/ first then copy, harness-share.md §22 reject halt: brainstorming retreat requires user decision, codex stuck threshold 30s→60s retry + 5min kill, harness-share.md §3 doc-only glob add CLAUDE.md/DESIGN.md, codex prompt skeleton "do NOT read"→"limit reads to repo under review", autonomous-loop git diff three-dot explicit intersection, harness-share.md §3 Gate 0 deepinit_manifest note, codex 'codex consult' trigger, pre-commit-gate remove redundant 'Gate 1.5' trigger, autonomous-loop "failed"→"fails", harness-share.md §22 halt clarified — version bumps: codebase-survey 1.2.5, web-loop 1.3.6, codex-cross-verification 1.0.8, autonomous-loop 1.0.5, pre-commit-gate 1.0.8 — queue: 0 remaining — PW: n/a harness-share.md §3 line 200 "5-gate/4-gate"→"6-gate/5-gate" (authoritative source self-contradiction), §26 heading "8-step"→"Step 0.5 + Step 1-8", §3 doc-only glob add skills/**/*.md, pre-commit-gate "6 gates in order"→"up to 6 gates" disambiguation + "run full 6"→"all applicable gates", autonomous-boundary "6-gate PASS"→"full PASS (6 or 5)", README gate note (Gate 0 conditional), pre-commit-gate 3-fail halt: autonomous-only qualifier, codex-cross-verification PIPESTATUS note — version bumps: pre-commit-gate 1.0.7, autonomous-boundary 1.0.6, codex-cross-verification 1.0.7 — queue: 0 remaining — PW: n/a harness-share.md §25 gate count 5→6, "8 steps"→"all steps (Step 0.5 + Step 1-8)" in codebase-survey/large-task-delegation/web-loop, pre-commit-gate "4 gates"→"all applicable gates", Explore→oh-my-claudecode:explore in large-task-delegation, pre-merge-sync "usually"→explicit phrasing, codex non-parseable stdout fallback, codex fallback overwrite rule, web-loop Q-WEBLOOP prefix convention, pre-commit-gate web-loop override exception cross-ref, test-coverage exit-code anti-pattern — version bumps: codebase-survey 1.2.4, web-loop 1.3.5, large-task-delegation 1.0.5, pre-commit-gate 1.0.6, pre-merge-sync 1.0.5, codex-cross-verification 1.0.6, test-coverage 1.0.4 — queue: 0 remaining — PW: n/a gate-count drift harmonized across pre-commit-gate/autonomous-boundary/web-loop/README (5→6, add Gate 1.5), git merge-base→git diff --name-only in autonomous-loop, reviewer FAIL halt added to pre-commit-gate failure protocol, deepinit_manifest fallback when OMC not installed, codex exit-code probe + error stub, pip--user + PEP 668 fallback in codebase-survey, web-loop moves/copies→copy, PR footer gate count disambiguation (no AGENTS.md case), reviewer FAIL count definition (per-reviewer or 3+ total), feature dir definition in codebase-survey, silent-skip anti-pattern in test-coverage, web-loop branch note — version bumps: pre-commit-gate 1.0.5, autonomous-boundary 1.0.5, web-loop 1.3.4, autonomous-loop 1.0.4, codebase-survey 1.2.3, codex-cross-verification 1.0.5, pre-merge-sync 1.0.4, test-coverage 1.0.3 — queue: 0 remaining — PW: n/a
Cycle 12 (2026-05-04 12:50) — [P0/P1/P2] 7 issues resolved: production-access SSM/SSM Session Manager triggers added + Interaction section (kzk-autonomous-boundary / kzk-user-queue / kzk-tool-retry cross-refs), autonomous-loop silent-resume anti-pattern (ScheduleWakeup wakeup must read harness-flow-progress + restate before dispatch), background-monitoring subagent completion verification section (receipt line after Agent() return, empty-result BLOCKED path, session-resume restatement mandate), codex-cross-verification prompt skeleton path guard: when design IS a skill the skills/ dir is the subject (self-improvement exception), web-loop gstack reverted to auto-install per user request (detect-only removed), README + CLAUDE.md production-access trigger row synced (SSM added) — version bumps: production-access 1.0.2, autonomous-loop 1.0.7, codex-cross-verification 1.0.11, web-loop 1.3.10, codebase-survey 1.2.9, pre-commit-gate 1.0.11, pre-merge-sync 1.0.7, user-queue 1.0.3, test-coverage 1.0.7, large-task-delegation 1.0.9 — queue: 0 remaining — PW: n/a
Cycle 13 (2026-05-04 13:20) — [P0x4] 4 real P0s fixed (4 false positives dropped): (1) web-loop Interaction §kzk-autonomous-boundary — reviewer FAIL override now correctly attributes kzk-autonomous-loop as source per §25; (2) autonomous-boundary + autonomous-loop — removed invented "pre-PR deepinit fails" halt condition not in harness-share.md §2; (3) codex-cross-verification — fallback description expanded to include parse-fail trigger (not just CLI unavailable); (4) pre-commit-gate — Gate 0 on new skill addition now conditional on AGENTS.md hierarchy presence — version bumps: autonomous-boundary 1.0.10, autonomous-loop 1.0.8, web-loop 1.3.11, codex-cross-verification 1.0.12, pre-commit-gate 1.0.12 — queue: 0 remaining — PW: n/a
Cycle 14 (2026-05-04 13:45) — [P0x2] 2 real P0s fixed (2 false positives dropped): (1) codex-cross-verification verdict file convention generalized — non-plan reviews (spec/arch/design/DB schema) now use docs/research/codex-reviews/<topic>-critic-review.md instead of hardcoded docs/plans/ path; error stub on parse-fail also updated to use §Verdict file convention reference; (2) pre-commit-gate frontmatter description "6-step" corrected to "Up-to-6-step" (Gate 0 is conditional) — version bumps: codex-cross-verification 1.0.13, pre-commit-gate 1.0.13 — queue: 0 remaining — PW: n/a
Cycle 15 (2026-05-04 14:10) — [P0x1+P1x1] 2 confirmed fixes: (1) autonomous-boundary frontmatter Required triggers missing "실행해놔야 queue 보지" and "끝까지 끝내줘" (body had them, frontmatter didn't — trigger detection divergence); (2) pre-merge-sync line 39 removed stale "halts the autonomous loop" claim for deepinit failure (deepinit was removed as halt condition in Cycle 13) — version bumps: autonomous-boundary 1.0.11, pre-merge-sync 1.0.8 — queue: 0 remaining — PW: n/a
Cycle 16 (2026-05-04 14:35) — [P0: none] 0 P0 — quality converged. 12 P1 and 16 P2 remaining (wording drift, cross-ref gaps, non-portable examples — no structural defects). Merging to main. — queue: 0 remaining — PW: n/a
Cycle 17 (2026-05-04 15:00) — [P1x7] 7 P1 fixes: 6 skills missing standardized `## Interaction with other kzk-*` hub section added (kzk-large-task-delegation, kzk-pre-commit-gate, kzk-pre-merge-sync, kzk-autonomous-loop, kzk-playwright-verification, kzk-user-queue) + kzk-web-loop frontmatter `12시간` trigger replaced with `무한 루프`/`계속 돌려` (loop is indefinite, not 12-hour-bounded — misleading triggers risk unintended overnight sessions); CLAUDE.md + README.md trigger rows synced — version bumps: large-task-delegation 1.0.10, pre-commit-gate 1.0.14, pre-merge-sync 1.0.9, autonomous-loop 1.0.9, playwright-verification 1.0.3, user-queue 1.0.4, web-loop 1.3.12 — queue: 0 remaining — PW: n/a
Cycle 18 (2026-05-04 15:25) — [P2x5] 5 P2 fixes: (1) kzk-codex-cross-verification line 88 fallback Agent() now includes `prompt=<same review prompt>` (copy-paste hazard); (2) same skill, codex-exec invocation switched from fragile embedded `python3 -u -c` to `jq -rR 'fromjson? | select(.text or .error) | .text // .error'` per global jq preference; (3) kzk-codebase-survey cross-ref to large-task-delegation now uses grep-stable §"Pre-implementation plan-critic loop (opus + codex)" anchor; (4) kzk-playwright-verification hardcoded `/auth/...` and `/your-protected-route` examples replaced with `<your-app-login-url>` placeholder + concrete examples (subdomain auth case included); (5) harness-share.md §2/§16 clarified `harness-test` is kzk-harness-internal convention only (drift with pre-merge-sync:11 resolved) — version bumps: codex-cross-verification 1.0.14, codebase-survey 1.2.10, playwright-verification 1.0.4 — queue: 0 remaining — PW: n/a
Post-cycle infra (2026-05-04 15:50) — Not a quality cycle, but install + workflow polish driven by user requests: (a) `install/dependencies.sh` + `install/dependencies.md` extracted — script auto-installs code-review-graph (pip --user → pipx) and codex CLI (npm → brew), detects gh + aws-vault, lists Claude Code plugins (`oh-my-claudecode`, `playwright-mcp`) for manual `/plugin` install. README install command step 5 now invokes the script; CLAUDE.md External Tools section condensed to a quick reference. (b) `.gitignore` extended with multi-editor MCP-registration artifacts that `code-review-graph install` creates (.mcp.json / .claude / .cursor / .kiro / .cursorrules / .opencode.json / .windsurfrules / AGENTS.md / GEMINI.md). (c) kzk-codebase-survey 1.2.11 — Step 1 now declares MCP → CLI → grep priority order; new `## MCP tool surface (preferred when registered)` section documents the code-review-graph MCP tool table (semantic_search_nodes, query_graph, get_impact_radius, etc.) so the guidance lives in the skill that uses it instead of bloating CLAUDE.md. (d) README.md `## Usage — starting a new feature` section added — 7-step end-to-end flow from spec draft → codebase survey → plan → branch dispatch → autonomous run → commit gates → PR/merge, each step listing its trigger phrase and the skill that auto-loads. — queue: 0 remaining — PW: n/a
Cycle 30 (2026-05-04 16:30) — [T1-T4 meta-gap closures] 트리거: 사용자 "TDD로 개발하게 시키고 있는거 맞지", "회귀테스트도 하고", "내가 어떻게 3개 파일 이상인지 몇라인인지 시킬 때 알 수가 없어 백프로 에이전틱 코딩", "git 관련 작업도 어려운 리베이스나 인터렉티브 같은거 아니면 하이쿠가 하게". (T1) TDD enforcement 강화 — `install/hooks/keyword-detector.mjs` R6 신규 추가 (kzk-test-coverage, triggers: tdd/TDD/test first/테스트 먼저/테스트부터/failing test/red-green/테스트 추가/테스트 추가해줘/test 추가/coverage 추가); kzk-test-coverage v1.1.0→v1.2.0 (description TDD-strict 강조, §Triggers 11 phrase 추가, §TDD sequence 신규 섹션: red→green→refactor→commit 시퀀스, 버그픽스 = 회귀 test first artifact, interactive mode TDD waive 조건); kzk-large-task-delegation §Subagent prompt requirements "TDD strict" → "TDD sequence (red-green-refactor — see kzk-test-coverage §TDD sequence; failing test BEFORE impl is non-negotiable in autonomous mode)". (T2) Scope estimation 신규 — kzk-large-task-delegation §Scope estimation 섹션 (Read-heavy audit 위에 삽입): 30초 추정 절차 (git status + dir scan + CRG query + LoC projection), 라우팅 결정 트리 (main-direct/haiku/sonnet/spec-and-review-first/opus), 1-line preamble 형식, mid-execution halt 룰; RULE 1 triggers에 7 vague-large phrase 추가 (리팩토링/refactor/정리해줘/cleanup/개선해줘/전반적으로/통째로) + "scope estimate" self-trigger. (T3) Git ops haiku tier — kzk-large-task-delegation §Tier triggers haiku 목록 끝에 git ops bullet (status/log/diff/show/stash list/branch/fetch/rev-parse/tag/add/restore/commit -m/fast-forward merge/non-conflict cherry-pick/push PR-flow) 추가; EXCLUDED 단락 (rebase -i/conflict resolution/reset --hard pushed/force-push/filter-branch/commit body authoring/blame semantic/branch contract decisions). (T4) README 갱신 — 6 RULES 표 (R6 추가, R1 triggers 확장), 메타 룰 6-7번 신규 (scope estimation + TDD sequence), 모델 표 heading Cycle 29→29-30 + Haiku 행 git ops 추가, 트리거 갭 테스트 추가 bullet 제거 + Cycle 30 close 노트. version bumps: kzk-large-task-delegation 1.3.0→1.4.0, kzk-test-coverage 1.1.0→1.2.0. branch contract: direct-main, no PR (Cycle 27-29 동일). — queue: 0 remaining — PW: n/a

Cycle 35 (2026-05-04 22:00) — [Plan F executed — PreToolUse edit-read-guard + install lib copy fix] cycle F (commit 375897e on feature/edit-read-guard). 사용자 명시 ("Edit 전 Read 안 하는 패턴 막아줘") 시스템적 차단 — PreToolUse hook 으로 Edit/Write 호출 시 같은 turn 안 Read 검증 → 자동 deny. Hook ESM resolve fail (lib 누락) 도 같이 fix. 신규: install/hooks/edit-read-guard.mjs (--mode=pre/post-read 단일 파일, realpath 정규화, tool_name 별 분기 — Write ENOENT allow / Edit read-required, single-use bypass token PreToolUse 단독 소비) + install/hooks/dispatcher.mjs (UserPromptSubmit canonical 통합: read-log clear → keyword-detector → regression-recall → fix-scope-trigger, manifest enabled.json 기반 활성 set, stale file 차단) + install/lib/turn-state.mjs (current-turn.json atomic + read-log.jsonl O_APPEND atomic — flock 폐기, NODE_ENV=test 게이트된 KZK_TEST_STATE_DIR override) + install/test/edit-read-guard.test.mjs (6 case). 수정: install-global.sh enable_hooks() canonical 재구성 (managed 파일명 whitelist 5개만 strip + 사용자 커스텀 hook 보존, install/lib/*.mjs idempotent copy with cmp -s) + uninstall-global.sh PreToolUse + PostToolUse + UserPromptSubmit + manifest 셋 다 cleanup + kzk-tool-retry v1.2→v1.3 §PreToolUse guard subsection ("guard 는 Read tool 만 인정, shell grep/cat tracker 안 잡힘") + harness-share §27.1 cross-ref. 41 PASS / 0 FAIL. spec rev1→rev3 frozen + Plan F rev1→rev2 frozen — codex CLI 두 번 모두 stdin path exit 0 + size cap (cycle 33 신규 룰) 적용. spec/Plan F cycle 1 의 13 항목 결함 모두 답 통합 (env 전파 약함 → on-disk turn state, TOCTOU → tool_name 별 분기, hook 순서 → dispatcher 통합, bypass 소비 주체 → PreToolUse 단독, enabled set → manifest, etc.). — version bumps: kzk-tool-retry 1.2→1.3 — queue: 0 — PW: n/a — branch: feature/edit-read-guard, no PR, main merge — next: kzk-pre-merge-sync → git merge --no-ff → global install update (사용자 환경 dispatcher 적용)

Cycle 34 (2026-05-04 20:30) — [Plan E executed — 마지막 plan, 5/5 완료] cycle E (commit dab0d55) — kzk-production-access v1.1→v1.2 (Permission model rewrite — read-only vs state mutation split / §Production state changes — code-first + 멱등성 / IaC-managed vs runtime-only 이분법 / 5 anti-pattern entries 확장 / authoritative source `harness-share.md §2`) + kzk-large-task-delegation v1.7→v1.8 (Production-code-first boilerplate sonnet executor dispatch inject) + kzk-pre-commit-gate v1.4→v1.5 (Gate 1.6 신규 — staged-path trigger / FAIL on direct shell exec / WARN on idempotency 부재 / [env-exception] escape / Up-to-9-step) + harness-share §2 하위 subsection (single SoT, destructive direct execution 룰 sync) + skill-text-checks 20+ Plan E grep 추가 + install/test/fixtures/gate-1.6-adhoc-grep.sh 신규 (3 fixture: FAIL/PASS/READ-ONLY) + run-tests.sh Gate 1.6 fixture call. 40 PASS / 0 FAIL. **5 plan 모두 executed (A→D→B→C→E):** Plan A e8a71967 / Plan D 53885de / Plan B 1ba063a / Plan C 3a57251 / Plan E dab0d55. codex CLI stdin path 5/5 회 성공. spec rev1→rev8 (사용자 명시 codex prompt size cap 추가). 신규 skill 2개 추가 (kzk-regression-memory, kzk-fix-scope-expansion → 14→16). cycle E sister bump (LTD 1.7→1.8, PCG 1.4→1.5) 사후 fix in this commit. — version bumps: kzk-production-access 1.1→1.2, kzk-large-task-delegation 1.7→1.8, kzk-pre-commit-gate 1.4→1.5 — queue: 0 remaining — PW: n/a — branch contract: feature/memory, no PR, main merge 단계 진입 — next: kzk-pre-merge-sync (CLAUDE.md sync + deepinit + step 3 자동 hook enable with 사용자 confirm) → git merge --no-ff to main

Cycle 33 (2026-05-04 19:30) — [Plan B/C executed + Plan E draft + Codex prompt size cap rule] cycle B (commit 1ba063a) + cycle C (commit 3a57251) executed. cycle B: kzk-fix-scope-expansion (16th skill) + fix-start hook (default DISABLED) + Gate 4.5 + hook-shared lib (D consumer drift 차단) + cache-write lockdir + retroactive D regression-recall 정정 + cross-skill (codebase-survey/pre-commit-gate/pre-merge-sync). cycle B sonnet task 11/15 미완성 → 메인 follow-up patch (install-global.sh `--fix-scope-trigger` flag + skill count 15→16 in install-global.sh). cycle C: kzk-large-task-delegation v1.6→v1.7 §Three-stage review (verifier dispatch + VERDICT 정규식 + thread halt) + kzk-pre-commit-gate v1.3→v1.4 Gate 5 (cache key triple) + kzk-autonomous-boundary v1.2→v1.3 halt 표 (Q-TDD-MAIN/VERIFIER-FAIL/INVALID/DISPATCH-FAIL, reason/action/resume schema) + harness-share §3 Gate 5 + §4 Stage 3 + verifier-routing.test.sh 12 case. 38→39 PASS. **spec rev7→rev8** + **kzk-spec-and-review v2.2→v2.3** (사용자 명시 cycle 33 — codex prompt size cap: prompt < 500 lines, Read 의무 ≤ 4 파일, 카테고리 ≤ 8, 응답 < 700 단어, plan 1500+ LoC 면 핵심 변경부만 발췌 inline. timeout 차단). Plan E draft (8 tasks, 12 AC, ~290 LoC) + codex review REVISE 11 항목 (SoT §17→§2, 권한 모델 reverse, Gate 1.6 grep 허술, IaC vs runtime 이분법, forward-only state semantics, AI access lock, Three-stage review reference, fixture test, rollback 2-3 level, trigger staged path 좁힘). Plan E rev2 background revise. — version bumps: kzk-large-task-delegation 1.6→1.7, kzk-pre-commit-gate 1.3→1.4, kzk-autonomous-boundary 1.2→1.3, kzk-spec-and-review 2.2→2.3 — queue: 0 remaining — PW: n/a — branch contract: feature/memory, no PR, 5 plan 끝나면 main merge — next: Plan E rev2 → frozen → cycle E → main merge (kzk-pre-merge-sync step 3 자동 hook enable)
Cycle 32 (2026-05-04 18:30) — [Plan D executed] kzk-regression-memory 신규 skill (15th) + recall hook (default DISABLED at commit) + sidecar 7-field schema (key/file_snapshot/related_cycles/dismiss_count/last_dismissed_at/archived/stale) + dismiss CLI + atomic sidecar writer (`install/lib/sidecar-write.mjs` — lockdir+tmp+rename) + stale-check + cycle 회고 통합 5W1H + cross-skill 변경 (kzk-pre-merge-sync §3 자동 enable + kzk-web-loop §5.5 cycle retro + kzk-large-task-delegation recall inject 200 char size cap) + gstack auto-install (npm-first → brew-fallback) + harness-share §29 신규. commit 53885de on feature/memory branch, 17 files +1011/-21, run-tests.sh 37 PASS / 0 FAIL (regression-recall.test.mjs 26 PASS 포함). gstack 미설치 환경 → recall feature OFF (hook silently no-ops + stderr WARN + structured `_warn`), cycle retro WARN degraded mode. version bumps (cycle D follow-up): kzk-pre-merge-sync 1.2→1.3, kzk-web-loop 1.4→1.5, kzk-large-task-delegation 1.5→1.6 (sonnet executor 가 SKILL.md 변경 했지만 frontmatter version 미증가 — 본 cycle entry commit 에서 사후 fix). spec rev6→rev7 wording 정정 (sidecar 6→7필드 stale 승격, Plan D rev2 와 sync). codex CLI stdin path 두 번째 성공 (Plan D rev1 review, REVISE 11 항목 + 1 none, all answered in rev2). — version bumps: kzk-regression-memory 1.0.0 신규 / kzk-pre-merge-sync 1.2→1.3 / kzk-web-loop 1.4→1.5 / kzk-large-task-delegation 1.5→1.6 — queue: 0 remaining — PW: n/a — branch contract: feature/memory, no PR, 5 plan 끝나면 main merge — next: Plan B draft (background) → cycle B → Plan C → Plan E → main merge
Cycle 31 (2026-05-04 17:48) — [Major spec rev1→rev6 + Plan A executed + Plan D draft] regression-memory-and-fix-quality-spec 5 critic cycle (결함 수렴 12→8→7→4→0 SHIP). 사용자 명시 추가: Axis E (production code-first + 멱등성, prod 직접 변경 시 마이그레이션 drift 방지). 진행 순서 A→D→B→C→E. cycle A executor sonnet dispatch — kzk-test-coverage v1.2→v1.3 (Anti-pattern Test-from-implementation + Layer b 자율 mode 메인 직접 TDD 금지 + 4 ACK 문구 fixed) + kzk-large-task-delegation v1.4→v1.5 (Sonnet executor §Anti-self-verification boilerplate literal text) + harness-share §11.1 + install/test/skill-text-checks.sh 신규 (positive 13 + negative 2 grep) + run-tests.sh 갱신, 36/36 PASS, commit e8a71967 on feature/memory. spec rev6 의 자율 mode 판별 우선순위: KZK_AUTONOMOUS=1 우선, env unset 시 동사구만 (명사 단독 금지). codex CLI stdin path 첫 두 번 성공 (Plan A review exit 0 → REVISE 7 항목 답 → 1 round rule frozen; Plan D review exit 0 → REVISE 11 항목 + 1 none, dismiss/archive mutation path 부재 / Step 0 모순 / orphan cleanup race + atomicity / gstack silent skip / fixture drift 과함 / cross-skill bloating). Plan D rev2 background revise 진행 중 (planner opus). branch contract: feature/memory, no PR, 5 plan 끝나면 main merge. Spec critic verdict 파일 5개 + Plan A/D verdict 파일 2개 + regression-memory-hypothesis 1개 모두 docs/research/codex-reviews/ + docs/plans/ 에 저장. — version bumps: kzk-test-coverage 1.2→1.3, kzk-large-task-delegation 1.4→1.5 — queue: 0 remaining — PW: n/a — next: Plan D rev2 → codex cycle 2 → frozen → cycle D dispatch
Cycle 29 (2026-05-04 15:30) — [Token consumption cut — T1-T4] 트리거: 사용자 "토큰 사용량이 너무 많은데" + "모델 최적화도 했으면 좋겠어". (T1) 14 SKILL.md description 압축 ~75% — 600-870 char inline keyword dump → 1-2 문장 + top 3-5 triggers + "Body §Triggers for full list" 참조 (~200 char). 전체 keyword 보존: 각 파일 H1 + authoritative source 라인 바로 아래 `## Triggers` 섹션 신규 추가 (comma-separated full list). version bumps: autonomous-boundary 1.1.0→1.2.0, autonomous-loop 1.0.9→1.1.0, background-monitoring 1.0.4→1.1.0, codebase-survey 1.4.1→1.5.0, large-task-delegation 1.2.0→1.3.0, playwright-verification 1.1.0→1.2.0, pre-commit-gate 1.1.0→1.2.0, pre-merge-sync 1.1.0→1.2.0, production-access 1.0.2→1.1.0, spec-and-review 2.1.0→2.2.0, test-coverage 1.0.7→1.1.0, tool-retry 1.0.5→1.1.0, user-queue 1.0.4→1.1.0, web-loop 1.3.14→1.4.0. (T2) `install/hooks/keyword-detector.mjs` `buildReminder()` 8-15 line bullet list → single-line: `🚨 [kzk] LOAD before edit: <skills> (matched: '<phrases>')`. run-tests.sh 2 test assertions 갱신 (short-form + matched: presence). (T3) doc-only verify gating 추가: `kzk-pre-commit-gate §Doc-only patch policy` 신규 섹션 (Gate 1.5 + AC2 만 실행, full suite 는 cycle close 1회) + `harness-share.md §3` 끝에 `### Doc-only fast path` 서브섹션 추가. (T4) `kzk-large-task-delegation §Model routing` 전면 재작성 — 2-tier (sonnet/opus) → 3-tier (sonnet/haiku/opus). haiku tier (Cycle 29 신규): version bump / frontmatter rewrite / single-line config / lint follow-up / progress log append / atomic rename / trivial test scaffold. opus tier 유지 (plan/critic/verify/security). target distribution 50% sonnet + 30% haiku + 20% opus (기존 80%/20%). codex consult: haiku-tier skip, sonnet-tier optional, opus-tier mandatory. codex consult 이번 사이클 skip — 사용자 명시 비용 트레이드오프. (T4 add-on) §Opus thinking-level (effort) guidance 신규 서브섹션 — opus dispatch 가 default xhigh, drop-to-high 조건 (well-scoped plan / mechanical critic / pre-spec'd verify) + stay-xhigh 조건 (ambiguous plan / arch-changing 또는 security critic / semantic verify / 직전 round REVISE) 명시. 세션 CLI banner level 운영 가이드 추가. — tests: 31/29+2 PASS (short-form assertions 2 추가) — queue: 0 remaining — PW: n/a — branch contract: direct-main, no PR (Cycles 27/28 동일)
Cycle 28 (2026-05-04 14:50) — [Skill-load chain meta-gap prevention] — 트리거: 사용자 "kzk 로드하고 나머지 수정은 메인에서 직접하는 것 같은데 이게 맞아?" + "재발 방지 개선하는 자기개선루프 돌려보자". external-project 2026-05-04 transcript 분석으로 확정된 패턴: 메인이 `kzk-codebase-survey` + `kzk-autonomous-boundary` 만 로드 → codebase survey EXPLORER 1회 dispatch (정상) → 그 후 11+ 파일 직접 read + SingleSelectPopupEditor.tsx 3 edit + Playwright + docker rebuild **모두 메인에서 실행**. `kzk-large-task-delegation` 미로드 = trigger keyword gap ('사용성 버그', '여러 plan 으로 쪼개', '사이클 자율' 미매칭). 자기 자신에게 적용: 메인 = 오케스트레이션 + 검증 + commit, 실제 6 file edit + 200+ LoC 는 executor sonnet 에 dispatch. (1) **Trigger keyword 확장**: `kzk-large-task-delegation` description 에 12 phrase 추가 ('사용성 버그', '사용성 회귀', 'QA scan', '여러 plan 으로 쪼개', '플랜 여러개로 쪼개', 'plan 쪼개', '사이클 자율', '사이클로 자율', '사이클 돌면서', '버그들 모두', '모두 잡아줘', '모두 개선'). `kzk-spec-and-review` description 에 7 phrase 추가 ('플랜 만들', 'plan 만들', '여러 plan', '플랜 여러개', '메타 plan', 'meta plan', 'spec 만들'). (2) **Hook 활성화**: `install/hooks/keyword-detector.mjs` Cycle 26 inert scaffold (`detect() = []`) → 5 RULES / 50+ trigger phrase 매칭 / `hookSpecificOutput.additionalContext` 형식으로 system-reminder 강제 주입 풀 구현. RULES: large-task / codebase-survey-chain (survey 트리거 시 large-task-delegation 도 강제 로드) / spec-and-review / autonomous-boundary / self-improvement-loop (자가개선 트리거 시 spec-and-review + large-task-delegation + pre-commit-gate + autonomous-loop 4-set 강제 로드). (3) **Session-28 worked example body**: `kzk-large-task-delegation §Session-28 lesson (skill-load chain)` 신규 섹션 — external-project transcript 그대로 + skill-load chain 룰 + Operational checks 1-4 (메인 turn 시작 전 점검: 트리거 phrase / 5+ file read / 3+ file edit / 200+ LoC). (4) **harness-share.md §28** 신규 — Skill-load chain 메타 갭 방지 룰 (survey + delegation 동반 로드 의무 + hook 자동화 + Session-28 anti-pattern 명시). (5) **CLAUDE.md self-trigger reminder** 2 row 추가 — "skill-load chain" + "사용자 prompt large-task signal 시 keyword-detector hook 활성화". (6) **install/test/run-tests.sh 5 unit test 추가** — large-task phrase / Session-28 phrase / multi-skill match / self-improvement chain (4-skill 동시 로드) / null-match continue:true. 29/29 PASS (24 기존 + 5 신규). 회귀 검증: verify-install AC2/3/6/7 4/4 PASS. (가짜 fail 1건: install/test + verify-install 병렬 실행 시 `/tmp/kzk-install-global.lock` race — 의도된 R-PLAN-3 lock guard 작동, 회귀 아님. 메모: install-touching tests 는 sequential 실행 의무.) — version bumps: kzk-large-task-delegation 1.1.2→1.2.0, kzk-spec-and-review 2.0.1→2.1.0 — queue: 0 remaining — PW: n/a — branch contract this session: direct-main, no PR (Cycle 27 contract 동일 scope) — next: 글로벌 install --update 로 ~/.claude/skills/ 동기화 + (옵션) install-global.sh --enable-hooks 로 keyword-detector 활성화

Cycle 27 (2026-05-04 14:30) — [Branch-contract policy shift + AC6 uninstall byte-identity fix + README install UX 통합 + CRG self-bootstrap] — 트리거: 사용자 "자가개선하자. main에 바로 커밋. 자율실행/하네스든 브랜치 만들지·이름 확인부터 받기. PR 항상 필수 X" + "글로벌 인스톨 복붙 한 섹션, GLOBAL(권장)/PROJECT-ONLY 선택" + "CRG 적극 사용 맞지". 메타 갭 발견: kzk-harness 레포 자체에 CRG 인덱스 0 (Cycles 20-26 글로벌 install 작업 동안 `--skip-project` 로 build skip 됐고 이후 self 부트스트랩 안 함). 해결: `code-review-graph build` 실행 → 79 nodes / 943 edges / 8 files (bash + js, install/ 트리). (1) **Branch policy shift**: 기존 "all edits on `feature/<topic>` 강제, main merge 명시 후" → "ASK FIRST 3 슬롯 contract (별 branch vs 직접 / branch 이름 / PR 여부) 사용자 명시 답 후 진입, contract 가 세션 끝까지 operating contract, direct-main 은 사용자 명시 인가 시만, PR 필수 X, force-push / reset --hard 는 contract 무관 별도 OK 사인". 8 군데 동기화: CLAUDE.md "Autonomous Execution Boundary" 본문 교체 + Self-trigger reminder 에 "branch contract 확인" / "CRG 부트스트랩" 2 row 추가, harness-share.md §2 line 61 (강제 금지 첫 항목 contract 항목으로 교체) / §8 line 428 (main 직접 push 금지 → contract 따름) / §15 line 533 (deepinit target PR-flow vs direct-main 분기) / §22 line 732 (codex prompt branch convention contract 반영) / §25 line 886 (web-loop branch boundary contract 반영), kzk-autonomous-boundary §Forbidden actions 위에 §Branch contract — ASK FIRST 신규 섹션 + Forbidden actions 재구성 (direct-main 조건부 허용, force-push/reset-hard/PR auto-merge 명시 금지) + Halt conditions §main access 항목 contract 인식 + §Branch policy detail 4 가지 contract shape 열거, kzk-pre-merge-sync 11 line opening (PR-flow + direct-main milestone 양쪽 커버) + 36-37 line target ("PR-flow 면 PR 직전, direct-main 면 milestone 직전") + 50 line PR footer checklist, kzk-pre-commit-gate §Autonomous-mode commit policy line 85 (push contract 따름), kzk-large-task-delegation Rules block branch boundary contract 반영, kzk-web-loop §Branch step 25 line ("자동 feature/web-loop-<slug> 만들기 X, ASK FIRST contract 후 진입"). (2) **AC6 FAIL fix**: `install/lib/claude-md-marker.sh` `claude_md_strip_block` awk 가 BEGIN 직전 빈 줄 1행 (inject 가 separator 로 항상 추가하던 것) 같이 소비하도록 lookahead 1 line buffer 추가. verify-install.sh AC6 PASS 회복 + AC2/3/7 그대로 PASS = 4/4. install/test/run-tests.sh 19/19 회귀 0. (3) **README Install 통합**: 기존 "Recommended global" + "Project-only legacy" 두 섹션 → 단일 복붙 prompt (사용자에게 GLOBAL vs PROJECT-ONLY 묻고 분기 실행). + "code-review-graph indexing is per-project" 단락 명시 (사용자 질문 반영 — 바이너리 글로벌 1회, SQLite 인덱스 프로젝트별, 글로벌 install 은 `--skip-project` 라 build 안 함, 각 프로젝트 첫 kzk-codebase-survey 트리거 시 자동 build). (4) **CRG 자기 부트스트랩**: 위 발견된 메타 갭 즉시 닫음 + Self-trigger reminder 매트릭스에 "메인이 cross-cutting 검증 필요 시 → CRG 우선, grep fallback. 인덱스 없으면 build 부터" 명시. — version bumps: kzk-autonomous-boundary 1.0.12→1.1.0, kzk-pre-commit-gate 1.0.15→1.1.0, kzk-pre-merge-sync 1.0.10→1.1.0, kzk-large-task-delegation 1.1.1→1.1.2, kzk-web-loop 1.3.13→1.3.14 — queue: 0 remaining — PW: n/a — branch contract this session: direct-main, no PR (사용자 명시) — next: 글로벌 install --update 로 ~/.claude/skills/ 동기화

Cycle 26 (2026-05-04 21:00) — [Major implementation] Plan Tasks A-G executed via 6 parallel/sequential subagent dispatches. 첫 실제 코드 commit (Cycles 20-25 는 spec/plan/skill markdown only). 진행: (병렬 1차) Task D (sonnet) `dependencies.sh --skip-project` 10 LoC + manual smoke test doc + shellcheck clean / Task C (sonnet) README rewrite (78→107 lines, 글로벌 install 우선 + legacy preserved + bash<(curl) drift 제거) / Task G (sonnet) `install/AGENTS.md` 133 lines + 7 TODO marker. (순차 2차) Task A (sonnet ~9분) `install/install-global.sh` 720 LoC + `lib/claude-md-marker.sh` 78 LoC + `UMBRELLA-README.md` 84 lines + `hooks/keyword-detector.mjs` 41 lines (inert scaffold) + `test/run-tests.sh` 269 lines / 10 tests PASS — mkdir-based lock (macOS flock 미지원 우회), multi-checkout SOURCE_REPO_DIR refusal, OMC keyword-detector collision 비차단 경고. (병렬 3차) Task B (sonnet) `uninstall-global.sh` 397 LoC + 3 uninstall test (OMC byte-equal preserve, marker strip, skill removal) → 15 tests PASS / Task E (opus per critic) `verify-install.sh` 497 LoC + `lib/precedence-probe.sh` 197 LoC + 4 verify test → 19 tests PASS — AC5 critic-corrected stream-json 파이프라인 사용 (`--output-format stream-json --verbose | jq -r 'select(.type=="assistant") | .message.content[]? | select(.type=="tool_use" and .name=="Read") | .input.file_path'` + dual FAIL count≥5 OR src/app/lib path), AC1/5/8 claude 미설치 시 SKIP 보호, AC8 probe trap-cleanup 다중 방어. (4차) Task F (sonnet) pre-merge verification 보고서 `docs/harness/surveys/2026-05-04-kzk-global-install-pre-merge.md` 279 lines — 9 sections (branch state, gate-PASS rendering, AC1-8 coverage, 19 test summary, AGENTS.md drift, manual smoke test, 11 deferred items, PR readiness checklist, rendered footer). 추가: `.gitignore` `AGENTS.md`/`GEMINI.md` → `/AGENTS.md`/`/GEMINI.md` (root-only) — Cycle 19 add 의 의도는 code-review-graph 가 root 에 자동 생성하는 파일 ignore 였지만 패턴이 너무 넓어 의도적 `install/AGENTS.md` 도 ignore 되던 것을 root-anchored 로 좁힘. 모든 shellcheck CLEAN (5 shell files), 19/19 tests PASS, 0 secrets. — version bumps: 없음 (skill 변경 없음, install/ 새 코드 + doc 만) — queue: 0 remaining — PW: n/a — next: 사용자 컨펌 → execute (실제 `~/.claude/skills/kzk-*/`, `~/.claude/CLAUDE.md` 글로벌 변경)
Cycle 25 (2026-05-04 19:30) — [Major plan authoring + critic-revise] kzk-harness 글로벌 설치 plan 작성 + frozen. spec frozen 입력 → planner opus draft → 병렬 codex CLI consult + critic opus review → critic-revise pass → frozen. 진행: (Step 1) planner opus draft → `docs/plans/2026-05-04-kzk-global-install.md` (781 lines, 6 tasks A-F + AC1-AC8 verbatim, 5 codex challenge points). (Step 2 codex CLI) `codex exec ... -s read-only --json` 5분 타임아웃 (EXIT=124), 7 agent_message progress 리포트만 받음 — verdict 미생성. (Step 2 critic-opus 병렬) full review → 🟡 REVISE: 1 CRITICAL + 5 MAJOR + 2 MINOR. 핵심 발견: AC5 jq query 깨짐 — `claude -p ... --output-format json` 은 `{type:"result", result:"..."}` 반환, `.messages` 필드 없음 (empirically verified). AC5 verifier 가 silent always-pass 상태 = 사용자 우려한 "main reads code weirdly" 패턴이 그대로 새어나감 = spec 존재 의의 자체. (Step 3 EXECUTOR sonnet revise): (a) AC5 jq → `--output-format stream-json --verbose | jq -r 'select(.type=="assistant") | .message.content[]? | select(.type=="tool_use" and .name=="Read") | .input.file_path'` + dual FAIL (count ≥ 5 OR src/app/lib 경로 매칭); (b) Task A stub 함수 (`claude_md_extract_block/_strip_block/_inject_block`) 와 미정의 `enable_hooks()` 본문 작성 — atomic awk + mktemp+mv + jq settings.json patch ~50 lines bash; (c) §5 dispatch order "D + A 병렬" → "D 순차 → A" (A의 Step 7 이 D의 `--skip-project` flag 소비, 순서 역전 시 positional arg 처리되어 integration test 깨짐 — `dependencies.sh:14` 실측); (d) `--ac8-attested-by-user "<DATE> probe-attested"` flag 추가 (CI 샌드박스 / fresh machine 에서 AC8 INCONCLUSIVE deadlock 회피, 타이핑 컨펌으로 silent 방지); (e) AC4 cleanup prose → bash trap (`git checkout -- harness-share.md`) 으로 dirty tree 방지; (f) Task G `install/AGENTS.md` seed 추가 (install/ 에 6 새 파일 + 기존 3 파일 = 9 파일 future Gate 0 baseline). 최종 plan 863 lines, status=frozen, ## Frozen 본문에 revision 요약. critic-opus 가 system instruction "Do NOT Write report .md" 로 인해 verdict 를 메시지로만 반환 → EXECUTOR 가 별도 verdict file `docs/research/codex-reviews/kzk-global-install-plan-critic-review.md` (98 lines) 작성. — version bumps: 없음 (plan/verdict 작성, skill 변경 없음) — queue: 0 remaining — PW: n/a — next: feature 브랜치 push + PR 생성 → 사용자 컨펌 → execute (Cycle 26: AC8 probe → Task D → A → B → C → E → F → G)
Cycle 24 (2026-05-04 18:35) — [Major spec authoring] kzk-harness 글로벌 설치 마이그레이션 spec 작성 + frozen. 사용자 직접 요청: "kzk 설치 스크립트를 글로벌로 + gstack/superpowers/omc 같은 트리거 메커니즘 + docs/harness 등 진행 문서는 프로젝트 루트 유지." 메타 갭 (kzk-* 가 다른 레포에서 트리거 안 됨) 의 1차 원인 = `~/.claude/skills/kzk-*` 미등록 (Cycle 22 트리거 분석 agent 가 evidence 확정). 진행: (Step 0) codebase survey 두 background agent 병렬 — explore sonnet (트리거 분석, 5 axes) + document-specialist sonnet (gstack/superpowers/omc install 메커니즘 비교). (Step 1) planner opus draft → `docs/plans/2026-05-04-kzk-global-install-design.md` (406 lines, 16 sections, 5 codex challenge questions). (Step 2) codex CLI consult `codex exec ... -s read-only --json` 5분 타임아웃 (EXIT=124), 89 jsonl line partial output 만 회수 — verdict 미생성. §Codex execution shape fallback 적용 → `oh-my-claudecode:critic` opus 단독 review. (Step 3) critic opus verdict → `docs/research/codex-reviews/kzk-global-install-critic-review.md` (200 lines, 🟡 REVISE) — 3 must-fix + 3 nice-to-have. EXECUTOR sonnet 적용: (1) §6.1 umbrella `~/.claude/skills/kzk-harness/` → dotfile `~/.claude/skills/.kzk-harness-shared/` (gstack의 umbrella가 SKILL.md를 갖는 것과 다르게 kzk umbrella는 shared assets only이므로 auto-scan 제외); (2) AC8 precedence-probe 추가 (project vs global skill 우선순위는 가정 아닌 실측); (3) §10 self-trigger matrix row 1에 `kzk-codebase-survey (Step 0.5 + 1–8)` 선행 — harness-share.md §26 ordering 반영; (4) AC5 fuzzy prose → `jq` Read-count shell test (≤4 threshold, P0 escalation on ≥5); (5) §7.2 install-global.sh 에 OMC `keyword-detector.mjs` 충돌 감지 step 추가 (ralph trigger 가로챔 경고, non-blocking); (6) §8.2 symlink-mode 역전 — `harness-share.md` 만 symlink, SKILL.md 는 file-copy + 명시적 `--update` 제스처 (WIP feature 브랜치 SKILL.md 가 글로벌로 leak 방지). 최종 spec 413 lines + `## Frozen` heading + frontmatter `status: frozen` + `revision: codex-timeout-critic-opus-fallback-applied`. — version bumps: 없음 (spec 작성, skill 변경 없음) — queue: 0 remaining — PW: n/a — next: plan 작성 (Cycle 25)
Cycle 23 (2026-05-04 18:05) — [P1x3 트리거 보강] Cycle 22 트리거 분석 결과 (d) "여전히 누락된 표현" 반영. 사용자 자연 발화 "스펙파일 ralph로 하나하나 체크 / 마무리 해줘 / 끝내줘" 등 표현이 매칭 안 되는 갭 닫음. 변경: (a) kzk-codebase-survey description 트리거 추가 — 'spec 체크', '스펙 체크', '하나하나 확인', 'ralph로 체크'; (b) kzk-large-task-delegation description 트리거 추가 — '마무리 해줘', '전수 검토', '끝내줘'; (c) kzk-autonomous-boundary description 트리거 추가 — 'ralph로 체크', 'ralph로 확인' (기존 'ralph로 돌려' 와 phrase variant); (d) CLAUDE.md + README.md 트리거 표 3행 동기화. 트리거 분석 agent (sonnet) 가 사후 검증한 (e) 권장 액션 — "글로벌 등록" / "~/.claude/CLAUDE.md 자동 머지" — 는 별개 spec 으로 진행 (Cycle 24 글로벌 설치 spec). — version bumps: codebase-survey 1.4.1, large-task-delegation 1.1.1, autonomous-boundary 1.0.12 — queue: 0 remaining — PW: n/a
Cycle 22 (2026-05-04 17:50) — [Meta P0] 자가개선 루프 self-trigger 갭 닫음. 사용자 직접 피드백: "이번 자가개선 cycle 자체에서 kzk-* 스킬이 하나도 트리거되지 않았다." 메인 컨텍스트가 EVALUATOR(critic)/EXECUTOR(executor) 만 dispatch하고 자체 kzk 스킬 워크플로우(spec → codebase survey → large-task delegation routing → pre-commit gate)를 건너뜀. 변경: (a) CLAUDE.md `## Self-Improvement Loop` 트리거에 `"자가개선"`, `"자가개선 돌려줘"` 추가 — 사용자 자연 발화 매칭; (b) 같은 섹션에 `### Self-trigger reminder (메타 갭 방지)` 신설 — 5개 매핑 명시: EVALUATOR/EXECUTOR dispatch=`kzk-large-task-delegation §Model routing`, 메인 5+ 파일 read=`kzk-large-task-delegation §Read-heavy audit dispatch shape`, 메타 작업(글로벌 install 등)=`kzk-spec-and-review` Step 0–3, cycle commit=`kzk-pre-commit-gate`+`kzk-pre-merge-sync`, 다중 cycle=`kzk-autonomous-loop`+`kzk-autonomous-boundary`; (c) "자가개선 루프가 자기 스킬을 안 쓰는 패턴 = 메타 갭 → 즉시 다음 cycle P0" 명시. — 변경 파일: CLAUDE.md only — version bumps: 없음 (CLAUDE.md는 frontmatter version 없음) — queue: 0 remaining — PW: n/a
Cycle 21 (2026-05-04 17:25) — [User-driven P0] 메인 opus 가 광범위 read 를 직접 하는 패턴이 `kzk-large-task-delegation` 트리거에서 빠진 갭 닫음. 사용자 직접 피드백: 다른 레포에서 "스펙파일 ralph로 체크 + 버그 전수조사" 요청 시 메인 에이전트가 비싼 opus 로 30+ 파일 직접 read → 컨텍스트 포화로 결론 흐려짐 ("코드를 이상하게 읽음"). 기존 트리거는 "3+ file edits / 200+ LoC" 즉 edit 기준만이라 read-only audit 시나리오 트리거 안 됨. 변경: (a) `harness-share.md §4` Large Task 리스트에 "5개 이상 파일 read 가 필요한 검증·audit (스펙↔구현 매칭, 버그 전수조사, 기존 시스템 review) — read-only 도 메인 직접 read 금지" 추가; (b) `harness-share.md §26` When mandatory 리스트에 "Implementation verification (spec↔코드 매칭, 버그 전수조사, 기존 시스템 audit)" 추가; (c) `kzk-large-task-delegation` description 에 6 트리거 추가 ('read-heavy audit', 'spec verification', '구현 검증', '버그 전수조사', 'implementation audit', '5+ file read'), Large 정의에 "5+ files needing full read for verification/audit" 추가, 새 섹션 `## Read-heavy audit dispatch shape` 신설 — 메인 직접 read 금지, EXPLORER subagent (sonnet/haiku) + CRG MCP/CLI dispatch shape 명시; (d) `kzk-codebase-survey` description 에 7 트리거 추가, Run before 리스트에 implementation verification 추가; (e) CLAUDE.md + README.md 트리거 표 두 행 동기화. — version bumps: large-task-delegation 1.1.0 (minor — new triggers + new section), codebase-survey 1.4.0 (minor — new triggers + new mandatory case) — queue: 0 remaining — PW: n/a
Cycle 20 (2026-05-04 17:00) — [P0x4 + P1x3 + P2x1] 8 issues fixed: (1) kzk-web-loop Plugin Pre-flight used `claude plugin add <name>` x4 — `add` is not a `claude plugin` subcommand (verbs: install/enable/disable/uninstall/update); silent runtime failure on every loop start. Replaced with `claude plugin install` + prose fix; (2) install/dependencies.md:65 playwright-mcp install said `claude plugin add @playwright/mcp@latest` — doubly wrong (no `add` verb, npm package name not plugin name); aligned with dependencies.sh:153 form `claude mcp add playwright -- npx -y @playwright/mcp@latest`; (3) README.md:46 Gate sequence wrong — said `Gate 1 (secrets scan), Gate 1.5 (lint/typecheck)` but authoritative pre-commit-gate/§3 has `Gate 1 (ai-slop-cleaner), Gate 1.5 (secrets scan)`, no lint/typecheck gate exists; corrected full Gate 0–4 list; (4) README.md:44 halt threshold said `≥ 3 reviewer FAILs` — pre-commit-gate splits this `≥ 2 consecutive reviewer/critic FAILs OR ≥ 3 consecutive build/test FAILs on the same area`; replaced; (5) pre-commit-gate:107 internal contradiction (line 102 said ≥2, line 107 said ≥3 for same threshold) — line 107 corrected; (6) spec-and-review:7 authoritative source pointer expanded `§22` → `§22 + §22.5 (Step 0 survey precondition references §26)` to match Cycle 19 Step 0 addition; (7) pre-commit-gate:111 referenced nonexistent `kzk-web-loop §Override exception` — replaced with `§Failure Handling` (greppable heading); (8) spec-and-review:114 Artifact retention said `{topic}-{YYYY-MM-DD}.md` but §Verdict file convention (line 56) prescribes `<topic>-critic-review.md` (no date); replaced with reference to §Verdict file convention. EVALUATOR also reported zero stale `kzk-codex-cross-verification` references (Cycle 19 rename was clean) and no skill-count drift. False positives skipped: 4 (skill count, rename leakage, version drift, §22.5 alignment). — version bumps: web-loop 1.3.13, pre-commit-gate 1.0.15, spec-and-review 2.0.1 — queue: 0 remaining — PW: n/a
Cycle 19 (2026-05-04 16:30) — User-driven polish, 4 changes: (1) renamed kzk-codex-cross-verification → kzk-spec-and-review (v2.0.0) — old name read like a codex adapter, but the skill actually authors specs/plans/designs (Steps 1-3) plus cross-vendor review; description leads with authoring; triggers expanded ('spec 잡자', 'spec 작성', 'plan 작성', 'design draft'); 8 sister-skill cross-refs updated via sed + 1 prose fix in kzk-large-task-delegation:163; (2) Step 0 codebase survey precondition added to kzk-spec-and-review — drafting blocks until survey report exists for the topic (lookup: in-session ref → docs/harness/surveys/*-<topic>-* ≤7d → .web-loop/surveys/cycle-N), auto-triggers kzk-codebase-survey if missing, cites path in draft prompt as "Required reading"; harness-share §22.5 End-to-End Ralph Pipeline gains Step 0 codebase survey; kzk-codebase-survey "Run before" list extended; (3) status-oracle pattern for code-review-graph — kzk-codebase-survey 1.3.0 Step 0.5 renamed "Tool Availability + Index Verification", uses `code-review-graph status` (Files/Nodes/Edges parse) as authoritative oracle since build log is misleading (shows last incremental pass, can read "8 files" when full graph has 2000+ nodes); build if empty/stale → re-status verify → cache CRG_AVAILABLE/CRG_FILES/CRG_NODES; install/dependencies.sh changed from background-build to foreground-build + status-verify; (4) Edit/Write read-tracker prevention — kzk-tool-retry 1.0.5 new "Pre-emptive Read protocol" table with 6 invalidators (user message, system-reminder file change, sed/Write/formatter, agent dispatch return, /compact, >5 turns since last Read) + recovery for "modified since read" variant; harness-share §27 mirrors. Plus install instruction refresh policy (README): re-run handles stale renamed/removed skills + replaces entire CLAUDE.md kzk-* section. Plus install/dependencies.sh plugin detect (oh-my-claudecode via installed_plugins.json, playwright-mcp via .claude.json/.mcp.json grep). — version bumps: spec-and-review 2.0.0, codebase-survey 1.3.0, tool-retry 1.0.5, large-task-delegation 1.0.11, pre-merge-sync 1.0.10, background-monitoring 1.0.4 — queue: 0 remaining — PW: n/a
Cycle 36 (2026-05-06 16:33) — [User-driven P0] Phase 1 codex 호출 안정화 SoT 새 스킬 1장 (kzk-codex-handoff) 추가, kzk-spec-and-review §Codex execution shape + §Prompt size guideline 본문 분리, kzk-large-task-delegation §Pre-implementation plan-critic loop cross-ref, 사용자 보고 codex 에러 4종 (timeout 5min / 즉시 종료 / stdout 0 byte / 일반 실패) Fallback 사다리 + Preflight (E0) + Fresh subagent 호출 패턴 + Q-CODEX-DISPATCH-FAIL halt entry. 카운트 17 → 18. cycle 1 codex review 13 issues / cycle 2 4 critical inline fix. 메타-갭 차단 룰 (FIX-LABEL / Q-FIX-PIVOT-FAIL halt) Phase 2 cycle 분리. branch: main 직접 / PR X — queue: 0 remaining — PW: n/a
Cycle 37 (2026-05-06 16:43) — [User-driven P2] keyword-detector.mjs RULES (kzk-codebase-survey rule) 에 자연 발화 매칭 7개 추가: '코드 서베이' / '코드서베이' / '서베이 해줘' / '서베이해줘' / 'code survey' / '코드베이스 서베이' / '코드 survey'. SKILL.md frontmatter description Top triggers 동기화 ('코드 서베이' 추가). version 1.8.0 → 1.9.0. Stage 3 N/A — mechanical 1-rule-entry add. — queue: 0 remaining — PW: n/a
Cycle 38 (2026-05-06 17:04) — [User-driven P2] keyword-detector.mjs RULES 분리: '코드 서베이' 류 7 자연 발화 trigger 를 단순 survey rule (skills: ['kzk-codebase-survey']) 로 분리. 사용자 결정 — large-task-delegation 동반 의무 X (단순 survey 요청). 기존 fix-implying trigger ('codebase survey', 'spec 체크' 등) 는 그대로 large-task-delegation 동반 유지. session-28 룰을 user override 로 완화. — queue: 0 remaining — PW: n/a
Cycle 39 (2026-05-06 18:09) — [User-driven P1] kzk-codex-handoff SKILL.md v1.0.0 → v1.1.0. 외부 평가 5 개선안 중 #3 약어/사내용어 + #4 TBD 자기참조 불완전 fix. 22 약어 verified audit (sister skill description / harness-share.md §section / omc subagent definition 본문 read). inline gloss 7 위치 + §Glossary 12 entries + §Changelog 2 entries (cycle 36/38 historical) 신설. TBD → self-authoritative + Phase 2 §32 migrate 명시. Stage 3 N/A — mechanical 본문 update (logic change X). — queue: 0 remaining — PW: n/a
Cycle 40 (2026-05-06 19:07) — [User-driven P1] 18 kzk-* SKILL.md 일괄 정리: (1) 모든 SKILL.md frontmatter description 을 push tone + context-rich (100-150 word) 으로 rewrite — Anthropic 베스트 프랙티스 (trigger info description-only, body §Triggers redundant) 부합. (2) 18 SKILL.md body §Triggers 섹션 통째 제거 (-121 lines 합산). (3) Cluster B 삼각 순환 정리 — codebase-survey 가 fix-start hub, fix-scope-expansion + freshness-guard 는 cross-ref + lazy invoke (description 에 명시). (4) keyword-detector.mjs RULES — `fix 시작` trigger 가 codebase-survey 단독 rule 매핑. (5) kzk-codex-handoff description 에 Phase 2 §32 reference 명시 (critical HIGH). (6) kzk-autonomous-boundary §Halt conditions 표에 Q-CODEX-DISPATCH-FAIL row 추가 (critical MEDIUM). 18 SKILL.md frontmatter version minor bump. — queue: 0 remaining — PW: n/a
Cycle 41 (2026-05-06 19:53) — [User-driven P1] skill-creator full eval pipeline 18 스킬 (β scope). 18 evals.json (1 prompt + 5 assertions / 스킬) → 18 with-skill + 18 baseline subagent dispatch (sonnet) → grader subagent grading. 종합: with-skill 100% (90/90), baseline 46.7% (42/90), delta +2.67. 강력 차별 9 SKILL (delta ≥ 4): autonomous-loop / codebase-survey / large-task-delegation / pre-merge-sync / spec-and-review / pre-commit-gate / regression-memory / test-coverage / web-loop. marginal 3 SKILL (delta = 1: background-monitoring / codex-handoff / production-access — over-engineering 재검토 후보). cycle 40 description push tone + context-rich rewrite 효과 검증 PASS. .eval-workspace/ artifacts .gitignore — commit X. push 없음 (사용자 명시). — queue: 0 remaining — PW: n/a

Cycle 42 (2026-05-06 20:18) — [User-driven P1] harness-share.md §32 신설 — Code Quality Discipline (DRY/YAGNI/KISS + 모듈 깊이 + 베스트 프랙티스). mattpocock improve-codebase-architecture (LANGUAGE.md / DEEPENING.md / INTERFACE-DESIGN.md) 핵심 패턴 통합: Deletion test (N ≥ 2 호출자) / Depth = interface 속성 / Design It Twice (큰 구조 변경 시 3+ 안 병렬) / Dependency 4-tier 분류 / obsolete unit test 삭제. dispatch prompt boilerplate 자동 inject 의무 — kzk-large-task-delegation §Subagent prompt requirements + kzk-spec-and-review Step 1 (Draft). 두 SKILL frontmatter version minor bump (1.13.0→1.14.0 / 2.8.0→2.9.0). 사용자 결정: 모듈 신규 X 룰 완화 (Deletion test 통과 시 OK). push 자제 (자율 cycle 중). — queue: 0 remaining — PW: n/a

Cycle 43 (2026-05-06 20:31) — [Autonomous P1] 18 SKILL 본문 §32 자기-적용 audit + 압축. EXPLORER subagent audit 결과 (full report: docs/harness/surveys/2026-05-06-skill-self-application-audit.md, 511 lines): P0 2건 (Gate 4.5 + Gate 0.5 SoT 중복) / P1 1건 (autonomous-mode SoT 부재 — Q-AUTONOMOUS-SOT user-queue 등록). Batch A: kzk-pre-commit-gate 3곳 cross-ref 교체 (Gate 4.5 / 0.5 / 5 VERDICT, -27 LoC). Batch B: 5 SKILL cross-ref 교체 (autonomous-loop / pre-merge-sync / regression-memory / web-loop x 2, -28 LoC). Batch C: 11 YAGNI delete (~60 LoC). 합산 -163 LoC (2721 → 2558, 6%). cycle 42 §32 도입 직접 후속 — DRY redundancy 제거 + KISS verbose 압축 + obsolete §section 삭제. push 자제 (자율 cycle). — queue: 1 pending (Q-AUTONOMOUS-SOT) — PW: n/a

Cycle 44 (2026-05-06 20:58) — [Autonomous P1] harness-share.md §33 Autonomous-mode Detection SoT 신설 (Q-AUTONOMOUS-SOT RESOLVED). canonical 동사구 list (Category A 명시 trigger 8구 + Category B 자가-skip 6구 + 환경변수 priority 1 + Category C 일반 명사 단독 금지). 3 SKILL (test-coverage / regression-memory / autonomous-boundary) cross-ref. drift 차단 (canonical: "ralph로 돌려" 공백 없음 표준). Batch D 잔여 압축 (-19 LoC): autonomous-boundary §Branch policy detail / halt-table Action cells / pre-commit-gate §Doc-only 통합. net +33 LoC (§33 SoT 비용 +52 - cross-ref/Batch D -19). Stage 3 verifier 진행. push 자제 (자율 cycle 중). — queue: 0 remaining (Q-AUTONOMOUS-SOT RESOLVED) — PW: n/a

Cycle 45 (2026-05-06 21:02) — [User-driven P1] kzk-freshness-guard SKILL.md §File path reference resilience section 신설. 사용자 보고 패턴 fix: 명시 path not found 시 즉시 halt → repo root 부터 fallback lookup (git ls-files / find -not -path '*/node_modules/*' '*/.git/*'). 1 hit = path update + WARN / 다중 hit = closest 선택 + WARN / 0 hit = ERROR + Q-FILE-MISSING-<basename> user-queue. anti-pattern: 즉시 사용자 prompt / system-wide find / 다른 repo 탐색. frontmatter version 1.1.0→1.2.0 bump. push 자제 (자율 cycle 끝). — queue: 0 remaining — PW: n/a
Cycle 46 (2026-05-06 21:17) — [User-driven P1] CRG (code-review-graph) auto-refresh 룰 추가. 사용자 보고: "플랜 여러개 한 번에 실행할 때 + 작업에서 처음 CRG 부를 때 각각 update + 새로 읽기". 변경: (1) kzk-codebase-survey §Step 0.5 (c) stale threshold >10 → drift > 0 + first call + (f) Auto-refresh on first CRG call — drift > 0 시 `code-review-graph update` incremental + session cache update, (2) kzk-autonomous-loop §Multi-plan CRG refresh 의무 — 시작 시 full build + 각 plan 사이 incremental update, (3) kzk-large-task-delegation §Subagent prompt requirements §CRG refresh boilerplate cross-ref, (4) harness-share.md §3.5 CRG auto-refresh policy 신설. Skip env: KZK_CRG_NO_REFRESH=1. CRG CLI verify: `update` (incremental, --base BASE) + `build` (full) 모두 확인. version bumps: kzk-codebase-survey 1.11→1.12, kzk-autonomous-loop 1.3→1.4, kzk-large-task-delegation 1.15→1.16. push 사용자 결정. — queue: 0 remaining — PW: n/a + post-commit CRG rebuild 룰 (kzk-pre-commit-gate §Post-commit CRG refresh) + cache invalidate triggers (codebase-survey §Step 0.5 (e)) — 사용자 보고 보강. version bumps: kzk-pre-commit-gate 1.9→1.10, kzk-codebase-survey 1.12→1.13, kzk-autonomous-loop 1.4→1.5.

Cycle 47 (2026-05-07 02:25) — [User-driven P1, B2 + C1 + Y combined] 사용자 명시: "B2 / C1 / Y, 코드 서베이 제대로 시작 + 스펙 + 플랜 여러개 + 자율 실행, direct-main commit". 진행: (Step 0) kzk-codebase-survey EXPLORE subagent (sonnet) — `docs/harness/surveys/2026-05-07-cycle-47-b2-c1-queue-survey.md` 352 lines, 3 sub-scopes (B2 anchor + cross-ref / C1 14 mattpocock candidates HIGH=1 MEDIUM=5 LOW=8 / Y producer gap + raw echo audit). CRG rebuild on main (28 files, 205 nodes, 1965 edges). (Step 1) executor sonnet draft → cycle-47 spec 537 lines (§1-§11). (Step 2) codex CLI consult `--ephemeral` plain text mode (90,513 tokens, EXIT 0) → 12 findings → verdict `docs/research/codex-reviews/cycle-47-spec-critic-review.md`. (Step 3) synthesize 🔴 5 + 🟡 4 + ⚪ 3 → executor sonnet revision (cross-ref +5 sister skills, AC presence-only marker, §8 R5-R7 risks 추가, §9 §32 evidence 보강, harness-share §3.5 edit defer to Phase 3). Plans 3개 frozen 병렬 생성 (Plan B2 174 lines, Plan C1 198 lines, Plan Y 222 lines). (Plan B2 implementation) executor sonnet — skills/kzk-fix-scope-expansion/SKILL.md (repo+global) `## Fix layer pivot (Phase 2)` 섹션 +60 LoC + 사전 정의 (같은 방향 = same root-cause label, 실패 = test red OR symptom unchanged after fix, L0-L3 dictionary), version 1.3.0→1.4.0; skills/kzk-autonomous-boundary/SKILL.md 글로벌만 수정됨 → 메인이 repo drift fix (단일 row). (Stale meta fix) verify-install.sh AC2 hardcoded 14 → 18 + ac2_marker_and_14_rows → ac2_marker_and_18_rows + run-tests.sh 17→18 (×2) — cycle 46 추가된 18번째 skill (kzk-codex-handoff) 가 test infra 까지 동기화 안 됐던 stale gap. (Plan Y implementation) executor sonnet 2회 dispatch — harness-share.md §6 신규 포맷 (3 sections + checkbox + 시간 역순 + RESOLVED 대문자 통일), skills/kzk-user-queue/SKILL.md producer table +8 rows (Q-TDD-MAIN, Q-MAIN-DIRECT-EDIT, Q-VERIFIER-FAIL/INVALID/DISPATCH-FAIL, Q-CODEX-DISPATCH-FAIL, Q-PROD-CODE-FIRST-<TOPIC>, Q-FIX-PIVOT-FAIL) repo+global sync, skills/kzk-web-loop/SKILL.md L119 raw `echo Q-PLUGIN-PREFLIGHT >>` → section-aware write (sed 제거 placeholder + awk insert after `## OPEN`) repo+global sync, docs/harness/user-queue.md 신규 포맷 migration (Q-AUTONOMOUS-SOT → RESOLVED checkbox, NOT_USED 13 mattpocock entries 5 MEDIUM + 8 LOW). (Plan C1 implementation) executor haiku — git-guardrails-claude-code SKILL.md fetch from GitHub upstream (95 lines), `~/.claude/hooks/block-dangerous-git.sh` (verbatim spec §4.2, chmod +x), `~/.claude/settings.json` PreToolUse Bash matcher merge (preserve every other key). AC-C1-1/2/3 PASS — `git push --force` blocked, `git push origin main` approved. C1 = repo 외 변경 only (commit 없음). 4 commits to main: 2810866 (docs spec/plans/codex/survey 7 files +2013), ded824d (Plan B2 + stale fix 4 files +68/-9), 673beca (Plan Y 4 files +51/-5), [Plan C1 install 별도 commit 없음]. Gate 1.5 secrets PASS 모두. Gate 3 module test 40/41 PASS (skill-text-checks.sh pre-existing fail unrelated — kzk-test-coverage version + kzk-production-access version assertions stale). Gate 5 verifier (opus) ded824d + 673beca 양 commit VERDICT PASS — drift empty repo+global 모두, scope 누수 없음, Q-FIX-PIVOT-FAIL no double-add (Plan B2 = halt table + Phase 2 section, Plan Y = producer table — 3 distinct mentions). out-of-scope: Plan B1 (new SKILL kzk-fix-layer-pivot, KISS 으로 inline 선택), Plan A1 (autonomous-boundary + autonomous-loop merge, over-engineering), C1 MEDIUM cluster install (issue-tracker 미채택, NOT_USED 기록), G2/G3/G4 full model-bias 차단 룰 (계측 후 별도 cycle), harness-share.md §3.5 layer-pivot 전용 §N 신설 (Phase 3 defer). version bumps: kzk-fix-scope-expansion 1.3.0→1.4.0. push 자제 (사용자 결정). — queue: 0 remaining — PW: n/a

Cycle 48 (2026-05-08 17:11) — [User-driven P1 doctor + polite-stop incident] 사용자 보고 2건: (1) `/doctor` 결과에서 kanban-doctor / kzk-web-loop / retro / devex-review / benchmark-models / plan-devex-review / caveman 등이 description truncated — 측정 결과 글로벌 83 skill description 합 16,468 chars (kzk-* 18개가 13,183 = 80%), Claude Code session-load skill metadata budget 초과로 후순위 truncate. (2) "1로 자율실행해줘" 시 메인이 contract 3-slot Q 답변만 echo 하고 turn 종료 (polite-stop violation) — 사용자 "왜 이거 물어보고 바로 끝내버리는거야". 근본 원인: kzk-autonomous-boundary §Branch contract 와 kzk-autonomous-loop §Polite-stop ban 사이 bridging rule 갭 ("답 받자마자 같은 턴에 dispatch" 룰 미명시). 진행: (Pass A 메인 직접) skills/kzk-autonomous-boundary/SKILL.md §Post-contract continuation 신설 (echo one-liner → 같은 턴 dispatch tool 강제, AskUserQuestion-then-stop = polite-stop signature 명시), skills/kzk-autonomous-loop/SKILL.md §Polite-stop ban examples canonical 7-pattern list (AskUserQuestion-end / tool-result-then-wait / Plan-A-prompt / reviewer-1×-FAIL / build-1-2×-FAIL / dispatch-return-then-wait / /compact-then-stop). (Pass B executor sonnet dispatch) 18 skills/kzk-*/SKILL.md frontmatter description compress: 합계 13,183 → 5,568 chars (-58%, avg 733 → 309), 모든 description ≤ 366 chars (target ~250 ceiling 400), version minor +1 일괄. Body 16개 unchanged + 2개 (autonomous-boundary, autonomous-loop) Pass A section 추가됨. (Hooks 메인 직접 — earlier in same turn) install/hooks/keyword-detector.mjs autonomous-mode 룰을 [kzk-autonomous-boundary, kzk-large-task-delegation, kzk-autonomous-loop] 3종 번들로 변경 (was just boundary), '상세하게 봐줘' / '상세히 봐줘' / 'detailed analysis' triggers 를 codebase-survey 룰에 추가, 'kzk 자율실행' / 'kzk 자율 실행' phrase 추가; install/hooks/edit-read-guard.mjs Step 5.5 isAutoInjectedPath() 화이트리스트 (~/.claude/CLAUDE.md, project root CLAUDE.md, ~/.claude/projects/<id>/memory/MEMORY.md 자동주입 file 들 read-log bypass); CLAUDE.md kzk-codebase-survey trigger row '상세하게 봐줘' 등 추가. Gate 3 test 40/41 PASS (skill-text-checks.sh 7 pre-existing fails — TC version 1.3.0 pin / PA version 1.2.0 pin / Permission rev2 / state semantics / impacted schema / env unset / 명사 단독 — main HEAD 에서도 fail = 회귀 아님, 별도 cycle 처리 필요). Gate 5 verifier (opus, fresh subagent) VERDICT PASS, 8/8 ACs VERIFIED, 0 blockers. 1 commit to main: 3d7d6b0 (21 files +107/-40). Reinstall `install-global.sh --update --yes --enable-hooks` PASS — 18 skills synced 글로벌 propagate, version=3d7d6b0, runtime detect() test 두 prompt 모두 의도대로 매칭. 즉시 효과 검증: 이 turn 의 system-reminder skill list 에서 이전에 truncated 였던 7개 스킬 + 18개 kzk-* 모두 새 description 으로 정상 노출. 메모리 저장: feedback_polite_stop_after_askuserquestion.md (agent memory, MEMORY.md index 갱신), feedback_kzk_repo_global_skill_drift.md SoT-only rule 로 덮어씀 (이전 cycle 47 양쪽 동시 edit 룰 파기). out-of-scope: skill-text-checks.sh 7 pre-existing fail 정리 (별도 cycle), 다른 비-kzk skill description diet (gstack/superpowers — 통제 외), README.md skills 섹션 description sync (현재 README 는 description 미인용). version bumps: 18개 kzk-* 일괄 minor +1. push 자제 (사용자 결정). — queue: 0 remaining — PW: n/a

Cycle 49 (2026-05-08 17:30) — [User-driven P1 follow-up Cycle 48: A/B verification + 127-truncate root cause] 사용자 우려 2건: (1) "다이어트 해서 스킬 eval 구려진 거 아니야?" — 정량 검증 요청. (2) "글로벌 인스톨 했는데도 새 세션 켰을 때 127개 디스크립션 트렁케이트 에러가 계속 나네" — 다이어트 효과 부족 의심. 진행: (Pass A-D executor sonnet) docs/research/cycle-49-description-ab/ A/B harness 구축 — 12 test prompts (canonical 5 + near-canonical 3 + cross-skill-binding 2 + non-canonical paraphrase 2), Pass B hook detect() 6/12 fully covered (hook layer 미변경 → 다이어트 무관 pre-existing gaps), Pass C native description token-overlap proxy 17% degradation but symmetric pre/post (다이어트가 원인 아님 — 두 description 양쪽에서 동일 ranking shift), Pass D codex LLM judge top-5 shifted prompts mean delta +0.6 (NEW > OLD on average — codex 가 새 description 을 더 명확한 signal 로 판정). VERDICT: TARGETED_FIX. (Targeted fix 메인 직접) skills/kzk-large-task-delegation/SKILL.md description Triggers list 에 'ralph로 돌려' / '끝까지 끝내줘' 추가 (+34 chars), version 1.17.0 → 1.18.0. C1/C4 native-matcher proxy gap 닫음 — non-hook session 에서도 large-task-delegation 가 autonomous-mode 요청에 surface. (127 truncate root cause 조사) earlier `awk` 측정은 multi-line YAML (`description: |` / `>`) 인 skill 들을 undercount — Python yaml.safe_load 로 재측정: ~/.claude/skills 83개 32,864 chars (median 357 max 770, 79 over 200), ~/.claude/plugins 202개 29,454 chars (median 98 max 972, 42 over 200), **combined 285 skills 62,318 chars 121 over 200** ≈ 사용자 보고 127. kzk-* 다이어트 후 18개 270-366 chars range 모두, over-500 0개 over-1024 0개 — 127 truncate 기여도 0. 진짜 책임자: gstack (plan-tune 770, office-hours 757, kanban-ticket-manage 728, design-html 721, plan-devex-review 691) + plugin (insane-search 972, math-olympiad 704, insane-apply 646, insane-design 610). kzk-harness 통제 외. ROOT-CAUSE-127-TRUNCATE.md 작성 — 3가지 user 옵션 (plugin prune, upstream gstack diet, accept). out-of-scope: gstack/plugin description 다이어트 (kzk repo 아님), Claude Code 정확한 truncate threshold 측정 (외부 변수). 1 commit to main: 1d2b2cf (45 files +636/-2: SKILL.md fix + 41 research artifacts). Reinstall propagate 성공. version bumps: kzk-large-task-delegation 1.17.0 → 1.18.0. push 자제 (사용자 결정). — queue: 0 remaining — PW: n/a

Cycle 50 (2026-05-08 17:50) — [User-driven P0 regression: external-project session] 사용자 보고: "feature/user-queue-cleanup branch 에서 Update(~/work/external/external-project/docs/harness/user-queue.md) 'Error editing file' 후 메인이 3.5분 halt, '계속 시도해' 입력해야 재개. 잘못된 경로 파일이면 다시 읽어서 봐야하는데 아직도 이거 방어가 안됐네." Cycle 48 polite-stop 7-pattern list 의 8번 누락 — Edit/Write 1× FAIL → halt 패턴 미커버. doc-only enforcement (kzk-tool-retry) 부족 = forcing 메커니즘 필요. (Pass A executor sonnet dispatch) 5-file change SoT only: (1) install/hooks/edit-failure-retry.mjs NEW — PostToolUse hook for Edit|Write, tool_response.is_error 또는 5개 error pattern (String to replace not found / File has not been read yet / File has been modified since / Error editing file / File does not exist) detect 시 hookSpecificOutput 으로 forcing system-reminder 주입 (agent 가 시스템 리마인더 무시 못 함), kill switch OMC_SKIP_HOOKS=edit-failure-retry, fail-open on malformed payload. (2) install/install-global.sh update_hooks_canonical 에 PostToolUse Edit|Write 등록 (idempotent). (3) skills/kzk-tool-retry/SKILL.md 1.5.0→1.6.0 §Forcing mechanism (PostToolUse hook — Cycle 50) 신설 — 5 canonical error patterns + 2-consecutive-fail Q-TOOL 절차 명시. (4) skills/kzk-autonomous-loop/SKILL.md 1.6.0→1.7.0 §Polite-stop ban examples #8 추가 — "Edit/Write 1× FAIL → halt" + cross-ref kzk-tool-retry §Forcing mechanism. (5) install/test/edit-failure-retry.test.mjs NEW 6-case (Read no-op / Edit is_error / Edit string-not-found / Write success / malformed JSON fail-open / kill switch). Test runner 41 PASS (was 40), 1 pre-existing skill-text-checks.sh fail unchanged. SoT-only verified (diff -q show files differ). Reinstall propagate: hook_dest 에 edit-failure-retry.mjs copy, settings.json reconstruct PostToolUse Edit|Write hook 등록. 1 commit to main: ed8d01c (6 files +260/-5). version bumps: kzk-tool-retry 1.5.0→1.6.0, kzk-autonomous-loop 1.6.0→1.7.0. push 자제. — queue: 0 remaining — PW: n/a

Cycle 51 (2026-05-08 18:00) — [User-driven P1 follow-up Cycle 50] 사용자 보고: "extra usage 중이어서 자꾸 멈추는 것 같기도 한데, 이때는 ratelimit 무시하고 끝까지 돌리게 해줘. 다음 사이클로 넣자." 진단: kzk-autonomous-loop §Rate-limit polling 룰이 "Anthropic 5h window" 만 다루고 extra-usage / overage / pay-per-use tier 시 HUD 신호로 메인이 cost-driven polite-stop 빠지는 패턴 미커버. 진행: (메인 직접 단일 파일 edit, 1.7.0→1.8.0) skills/kzk-autonomous-loop/SKILL.md — (1) §Rate-limit polling 안에 §Extra-usage / overage tier — DO NOT halt 신설: HUD 신호 ('extra usage', 'overage', 'additional usage', 'pay-as-you-go', cost-warning) 들은 halt 조건 아님 — 사용자가 이미 autonomous run 권한 줬으니 cost 는 사용자 billing 책임. 5h-window strict rate-limit (요청 실제 실패) 만 ScheduleWakeup 발동. cost-only 신호는 절대 halt X. (2) §Polite-stop ban examples #9 추가: "Cost / extra-usage / overage signal → halt" + cross-ref. 8 → 9 canonical examples. (3) frontmatter description 수정: overage handling 명시 + '9 canonical examples in body' (Cycle 50 에서 7 → 8 drift 도 동시 보정). 1 commit to main: 새 commit (1 file +20/-3). version bumps: kzk-autonomous-loop 1.7.0→1.8.0. push 자제. — queue: 0 remaining — PW: n/a

Cycle 52 (2026-05-08 18:30) — [User-driven P0 follow-up Cycle 51] 사용자 핵심 질문: "HUD signal 로 충분해?" + "halt 키워드가 없어도 실제로 작업 끝났으면 멈춰야하는데 너무 과하지 않나". 진단: Cycle 51 의 declarative HUD-signal 룰만으로는 forcing 메커니즘 0 — agent 가 HUD 직접 못 봄 + cost-driven polite-stop 본능 살아있음. 하지만 단순 Stop block 은 over-aggressive — 진짜 완료 시점도 막히면 사용자 lock-in. 해결: smart completion-detection Stop event hook. 진행: (Pass A executor sonnet dispatch) 5-file change SoT only: (1) install/hooks/keyword-detector.mjs UPDATE — `~/.cache/kzk-harness/autonomous-active` marker 라이프사이클 관리 (autonomous trigger 매칭 시 setMarker, halt keyword 매칭 시 clearMarker). HALT_KEYWORDS = ['그만', 'stop autonomous', 'halt autonomous', '끝났어', '다 끝났어', '이제 그만' 등]. AUTONOMOUS_TRIGGER_PHRASES = ['ralph로 돌려', '자율실행', '끝까지 끝내줘' 등]. (2) install/hooks/autonomous-stop-guard.mjs NEW — Stop event hook with smart completion detection: marker TTL check (default 1hr, env KZK_AUTONOMOUS_TTL_SEC), TodoWrite open count from transcript_path, user-queue.md non-RESOLVED Pending count. open work detected → block stop with reason citing escape options ('그만' / TTL / kill switch). all clear → allow stop + auto-clear marker. Safety: per-turn max-block 3 (runaway 방지), kill switch OMC_SKIP_HOOKS=autonomous-stop-guard, fail-open on any internal error. (3) install/install-global.sh UPDATE — autonomous-stop-guard.mjs 를 update_hooks_canonical 에 등록 + Stop hook entry idempotent. enable_hooks 에 hook copy 추가. (4) skills/kzk-autonomous-loop/SKILL.md 1.8.0 → 1.9.0 §Stop event hook — forcing mechanism (Cycle 52) 신설. (5) install/test/autonomous-stop-guard.test.mjs NEW 7-case (no marker / TTL expired / all clear / open todos block / open Pending block / max-block escape / kill switch). KZK_MARKER_DIR_OVERRIDE env for test isolation. Test runner 42 PASS (was 41), 1 pre-existing skill-text-checks fail unchanged. SoT-only verified. Reinstall propagate 성공: hook copy + settings.json Stop hook 등록. version bumps: kzk-autonomous-loop 1.8.0 → 1.9.0. push 자제. — queue: 0 remaining — PW: n/a

Cycle 53 (2026-05-11 22:30) — [User-driven multi-topic harness self-improvement session, 1 자율실행 turn] 사용자 트리거 3개. (1) "코덱스 와따가따 = 패스 할 때 까지 반복하라고 명시했는데, 이거 스킬에도 추가해서 추가 프롬프트 없이 재현 가능하게" → spec-and-review §Pattern 이 단발 round 처럼 읽혀 메타 갭. (2) "방금 dev-server 가 stale 빌드 그대로 띄우는데 main 이 '다 됐다' 결론. Tailwind v4 @import order 위반으로 dev fail / prod pass — 어케 막을까. 프레시 에이전트 검증 좋아" → Gate 4 production-only 검증 + main self-verification 사각지대 + dev/prod 빌드 격차 트랩. (3) "Playwright OAuth click-through 가 multi-account / consent / popup 환경에서 그냥 멈춰버림. 타겟 이메일 명시 X, 멀티 계정이면 유저에게" → OAuth protocol 본문이 high-level + halt entry 1개 (NEW-ACCOUNT) 만. 진행 (3 logical sub-cycles, 1 atomic commit): (A) kzk-spec-and-review 2.11.0 → 2.12.0 + harness-share §22 — Pattern (3-pass) → Pattern (Iterative review loop) Cycle N 반복 + Gate decision PASS/CONTINUE/HALT (PASS = BLOCKER 0 + 구조 변경 X, CONTINUE = BLOCKER ≥ 1 OR 구조 변경, HALT = cycle ≥ 5 + BLOCKER 잔존 → user-queue), Verdict file cycle N 확장 (N≥3 → -critic-review-N.md), cycle budget default 5, anti-patterns 3개. (B) kzk-playwright-verification 1.5.0 → 1.6.0 + kzk-autonomous-boundary 1.8.0 → 1.9.0 + harness-share §3 Gate 4/5 — Dev/prod build divergence trap 신설 (Tailwind v4 @import order, Vite HMR fail, Next Turbopack stale chunk, dev server died but page cached) + Gate 4 step 0 dev server health pre-check (ps + log tail error grep + page.reload bypassCache) + Autonomous completion fresh-agent exit verifier (autonomous loop 종료 직전 oh-my-claudecode:verifier 의무 dispatch, main self-declared "다 됐다" forbidden, Q-COMPLETION-SELF-VERIFY halt entry). (C) kzk-playwright-verification 1.6.0 → 1.7.4 + kzk-autonomous-boundary 1.9.0 → 1.10.2 + harness-share cheatsheet — OAuth click-through protocol 전면 강화 via spec-and-review iterative loop (executor sonnet 4 cycles): §Pre-click target identification (5+5 labels EN/KO), §Popup OAuth detection (window.open + named/reused popup 2-phase tab match: parent_idx 제외, accounts.google.com first, callback path fallback), §Account picker page (N=0/1/≥2 분기 + "Continue as <name/email>" fast-path), §Consent / scope review (URL + DOM CTA detection, 4-page cap), §App callback verify (3-mode detection: query param / path list / popup-close + 10s session poll), §Stuck detection / escape hatch (7 COOP/COEP/popup signature patterns), 6 halt entries 양방향 cross-ref (NEW-ACCOUNT/MULTI-ACCOUNT/CONSENT-LOOP/STUCK/CHALLENGE/PROVIDER-ERROR), §Forbidden patterns 3 new (row-index cache 금지 / hardcoded XPath chain 금지 / consent CTA host verify), access_denied dual-cause note (config vs user-decline), CSRF state/nonce scope note. 4 codex verdict files: docs/research/codex-reviews/playwright-oauth-hardening-critic-review[-N].md (cycle 1 BLOCKER 9 → cycle 2 BLOCKER 5 → cycle 3 BLOCKER 1 → cycle 4 BLOCKER 0 PASS). Exit verifier (oh-my-claudecode:verifier opus) VERDICT PASS 6/6. LOCKED user decisions: target email 명시 의무 X, 5-7 labels per language, 6 halt entry cap. CLAUDE.md trigger keyword table 3 row sync. operating contract: branch=main 직접, no PR. push 자제. — version bumps: kzk-spec-and-review 2.11.0 → 2.12.0, kzk-playwright-verification 1.5.0 → 1.7.4, kzk-autonomous-boundary 1.8.0 → 1.10.2 — queue: 0 remaining — PW: n/a (skill doc only)

Cycle 56 (2026-05-13 22:50) — [P0 hardening — test isolation + chain command env parsing + global propagate] 사용자: "p0 두개부터 하고, 그리고 글로벌 업데이트 해줘. 이후에 커밋하고 푸시. 자율 실행." Cycle 55 dogfood 가 노출한 2 bug. (P0-1) Test side-effect: `tests/hooks/cycle-exit.test.mjs` 의 bypass test 들이 `appendUserQueue` 실행 → production `docs/harness/user-queue.md` write. Cycle 55 final commit 직전 2 entries manual cleanup 발생. (P0-2) parseInlineEnv 가 _command 전체의 시작_ 만 검사 → `git add ... && KZK_CYCLE_EXIT_VERIFIED=1 git commit ...` chain 의 마지막 sub-command env prefix 못 잡음. Cycle 55 final commit 시 우회로 단독 명령 강제. **Fix (executor sonnet 1 dispatch)**: (a) Hook `appendUserQueue` 가 `KZK_QUEUE_DIR_OVERRIDE` env 인식 (Cycle 52 `KZK_MARKER_DIR_OVERRIDE` 패턴 mirror) — fallback REPO_ROOT/docs/harness. Bypass tests 가 per-test `os.tmpdir() + mkdtemp` 디렉토리 사용 + cleanup. Production user-queue.md 무오염 확인. (b) `parseInlineEnv` 가 stripped command 를 top-level `&&` / `;` / `||` 로 split → 각 sub-command 시작에서 env prefix 시도 → merge results. **두 파일 동시 변경** (`.claude/hooks/` + `install/hooks/`). Tests 45 → 53 (3 queue isolation + 5 chain env parsing 새 케이스). Commit `209764e fix(hooks): P0 hardening — test isolation + chain command env parsing`. **Global propagate**: `bash install/install-global.sh --update --enable-hooks --yes` exit 0 — 18 skills sync (3 new versions: 1.7.0 / 1.9.0 / 1.14.0 + 15 preserved), umbrella harness-share.md, CLAUDE.md routing block updated. PreToolUse Bash matcher 에 `node .../check-cycle-exit.mjs` 자동 등록 (jq reconstruct). `~/.claude/skills/.kzk-harness-shared/hooks/check-cycle-exit.mjs` 12853 bytes deployed. repo ↔ global diff empty (3 SKILL.md). **Verifier dispatch** (oh-my-claudecode:verifier inherit-opus) — 4 sub-check PASS. Final marker commit + `git push origin main` (사용자 explicit OK this cycle — Cycle 55 contract 의 push 자제 default override). GitHub Pages auto-deploy 트리거 (docs/site/**). — version bumps: 없음 (Cycle 56 는 hook 본문 hardening 만, SKILL.md frontmatter 무변경) — queue: 0 remaining — PW: n/a — branch contract: main 직접, no PR, default propagate, **push 명시 OK**

Cycle 55 (2026-05-13 21:30) — [User-driven cycle-exit hook + 4 sub-check enforcement — cycle-25-regression prevention] 사용자 트리거: 외부 프로젝트 cycle 25 (grid-lock) postmortem 제시 — 6 회귀 (셀편집 frontend+backend / history UX style / lock stub / toolbar size drift / viewId undefined / Export border) 가 release 됨. 5 detection gap (G1 large-cycle exit fresh-agent verifier 부재 / G2 endpoint deletion 시 callsite sweep 안 함 / G3 dev/prod build divergence / G4 stub release 까지 살아남음 / G5 SoT wip lag) + "kzk-pre-merge-sync 안 쓰는데 commit/cycle 끝날 때 잡게 못해주나?" → hook-enforced cycle-exit gate 방향 결정. 사용자 interactive decisions (4 Q): marker scope = 명시 marker 3개만 (MILESTONE: / CYCLE-EXIT: / STUB-CLEAR: — cycle keyword 제외, agentic coding context), Signal C (SoT diff 감지) = 폐기 (각 프로젝트 SoT 경로 천차만별, sub-check 3 에서 처리), gh pr create --draft = BLOCK, prod-build smoke harness-context adapt = HTML/SoT alignment. (Pre-discovery) 글로벌 ~/.claude/skills/ 에 cycle-25 fix 3 skill 변경 이미 적용된 상태 발견 — 메모리 정책 (SoT = repo, mirror 는 install propagate) 위반. Plan §A 에서 reconcile. **Plan (docs/plans/2026-05-13-cycle-exit-hook-design.md, 688 LoC)**: codex review 4 cycles to PASS (cycle 1: 5 BLOCKER — marker contradiction / sub-check 4 ambiguity / install idempotency / §3 vs §14.5 SoT conflict / brainstorm ALL-of 위배 → cycle 2: RESOLVED 5 + 1 NEW-BLOCKER Signal A refspec → cycle 3: RESOLVED + 1 NEW-BLOCKER §B-1 잔존 → cycle 4: PASS). spec-and-review §22.5 alternate path "recorded evidence" (8 conversation turns + 4 interactive Q) 충족. Brainstorming evidence 본문 §2 에 enumerate. **Phase A** (b321404): 글로벌 → repo back-port — kzk-fix-scope-expansion v1.6.0 → v1.7.0 (API deprecation flow, G2) + kzk-pre-merge-sync v1.8.0 §5 Stub sweep (G4) + kzk-autonomous-boundary v1.13.0 Large-cycle exit trigger (G1 부분). **Phase B+C** (ad3ac5a): 신규 `check-cycle-exit.mjs` PreToolUse Bash hook (.claude/hooks repo-local + install/hooks propagation copy, ~10KB) — Signal A regex `\bgh pr (create|merge)\b` + `\bgit push\b.*\borigin\s+(?:[^\s:]+:)?main\b` (refspec form 포함) / Signal B multiline marker `^(MILESTONE|CYCLE-EXIT|STUB-CLEAR):` / stripQuotedAndHeredoc preprocess (Phase F dogfood 중 발견된 heredoc false positive 즉시 fix) / bypass env vars VERIFIED+SKIP+DISABLE+conflict / --status + --dry-run CLI. kzk-pre-merge-sync v1.8.0 → v1.9.0 §6 Prod-build smoke + §7 SoT alignment 신설. kzk-autonomous-boundary v1.13.0 → v1.14.0 §Cycle-exit mandate (4 sub-check + verifier dispatch prompt template). harness-share §3 Gate 6 (PRIMARY normative body) + STUB convention paragraph + §14.5 cross-ref-only. 39 unit tests pass (Signal A 13 + Signal B 13 + bypass 5 + edge 4 + heredoc false-positive 4). **Phase D+E** (59b0071): docs/site/skill-flow.html + .ko.html — Gate 6 mermaid 노드, 3 skill cards version bumps, self-trigger table 행, hook section (9 global + 1 project-only), fingerprint regen ceed3af220972e84 → f9a71bf946cd959a (FRESH). **Phase G** (4509752): install/install-global.sh default-propagate (managed whitelist 8 filenames, NO_CYCLE_EXIT_HOOK=0 default-on, --no-cycle-exit-hook opt-out, KZK_CYCLE_EXIT_DISABLE runtime opt-out) + install/uninstall-global.sh symmetric (보너스: 빠진 edit-failure-retry + autonomous-stop-guard 2 entries 동시 보강). **Phase F dogfood**: commit 3 첫 시도 → hook BLOCK (Signal A false positive on heredoc body) → main 진단 + sonnet executor fix (stripQuotedAndHeredoc helper 추가, 4 new tests) → retry PASS. 최종 marker commit: CYCLE-EXIT: cycle 55 — fresh-agent verifier (oh-my-claudecode:verifier opus) dispatch + 4 sub-check PASS → KZK_CYCLE_EXIT_VERIFIED=1 git commit (Signal B matched → bypass). branch contract: main 직접, no PR, default propagate. — version bumps: kzk-fix-scope-expansion 1.6.0 → 1.7.0, kzk-pre-merge-sync 1.8.0 → 1.9.0, kzk-autonomous-boundary 1.13.0 → 1.14.0 — queue: 0 remaining — PW: n/a (skill+hook+install)

Cycle 54 (2026-05-11 22:55) — [User-driven dual policy upgrade in 1 autonomous turn] 사용자 두 질문: (1) "자율실행에서 TDD 필수로 먼저 만들고 하는 거 맞지?" → 진단: 현재 kzk-test-coverage 가 'tdd' / 'test first' / '테스트 먼저' 키워드 명시 trigger 만 active, 자율실행 단독으로는 TDD strict 자동 X — 자율실행 중 코드 변경 시 TDD skip 빈번한 의도되지 않은 갭. (2) "브레인스토밍 안 부르는 거 같애, 트리거가 너무 복잡해서 안 불리는 건가?" → 진단: keyword-detector substring match 가 '자율실행' / '끝내줘' / 'ralph' task trigger 에도 brainstorm marker over-inject. spec-and-review §Step -1 본문은 "탐색적 키워드만 진입, 명확 task = Step 0 직행" — 두 룰 충돌 → main silent skip. LOCKED user decisions: (A) 자율실행 + 코드 변경 = TDD strict 자동, (B) "스펙앤리뷰에서 항상 부르게 해줘. 코드서베이하고 유저한테 물어볼것이 있거나 새 기능 추가 같은 경우에는 브레인스토밍 스킬을 항상 부르게 해줘". 진행 (3 codex review cycles, executor sonnet dispatch): (A) kzk-test-coverage 1.7.0 → 1.8.2 + kzk-autonomous-boundary 1.10.2 → 1.11.0 + harness-share §11.2 — 새 §Autonomous mode TDD enforcement section, code-file definition (matches NONE of kzk-pre-commit-gate doc-only fast-path patterns — config/schema files 포함), TDD evidence contract (3 artifacts: staged test file, scope overlap, runner exit 0), Q-TDD-AUTO-MISSING halt entry (autonomous-boundary table), Q-COV-SETUP precedence-blocking halt (kzk-test-coverage local — mutual-exclusion with Q-TDD-AUTO-MISSING). (B) kzk-spec-and-review 2.12.0 → 2.13.2 + harness-share §22 + §31 — §Step -1 default ON (Step 0 survey → Step -1 brainstorming → Pattern), skip semantics EITHER(A explicit `brainstorming 스킵` standalone) OR ALL-of(B=3: trivial + 사용자가 모든 detail 명시 + 새 capability 추가 X), mandatory skip evidence (commit footer 또는 user-queue 에 사용자 prompt 인용 + files + sections 기록 의무 — absence → skip invalid → brainstorming MUST run), mandatory invoke triggers (새 기능 / 사용자 결정 필요 / 명시적 brainstorm 키워드). (C) harness-share §33 — Categories A and B exhaust trigger universe (no Category C), 모든 cross-ref matrix + 본문에서 `+ C` 제거. 추가 collateral cleanup: kzk-regression-memory line 95 "Category B + C" → "Category B". 3 codex verdict files: docs/research/codex-reviews/tdd-auto-brainstorm-default-on-critic-review[-N].md (cycle 1 BLOCKER 5 + 2 NIT — skip OR/AND 모순 / code-file ALL→ANY / SoT boundary drift / evidence contract 부재 / Category C undefined → cycle 2 BLOCKER 3 + 1 NIT — §22 summary regression / config-schema 누락 / first-cycle test count → cycle 3 BLOCKER 0 + 1 NIT frontmatter wording → PASS). Exit verifier (oh-my-claudecode:verifier opus) VERDICT PASS 6/6. CLAUDE.md trigger keyword table 3 row sync (kzk-test-coverage + kzk-autonomous-boundary + kzk-spec-and-review). operating contract: branch=main 직접, no PR. push 자제. — version bumps: kzk-test-coverage 1.7.0 → 1.8.2, kzk-autonomous-boundary 1.10.2 → 1.11.0, kzk-spec-and-review 2.12.0 → 2.13.2 — queue: 0 remaining — PW: n/a (skill doc only)
