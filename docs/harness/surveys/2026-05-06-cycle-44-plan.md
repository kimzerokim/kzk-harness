# Cycle 44 Plan

Generated by: Explorer subagent (2026-05-06)
Source files read: kzk-test-coverage/SKILL.md, kzk-regression-memory/SKILL.md, kzk-autonomous-boundary/SKILL.md, kzk-pre-commit-gate/SKILL.md, harness-share.md, docs/harness/surveys/2026-05-06-skill-self-application-audit.md, docs/harness/user-queue.md

---

## Task 1 — Q-AUTONOMOUS-SOT

### 1A. 3 SKILL verbatim quote

#### kzk-test-coverage/SKILL.md — L59–64 (자율 mode 판별)

```
**자율 mode 판별** (spec rev6 wording 그대로 — `=0 override` 없음):
1. 환경변수 `KZK_AUTONOMOUS=1` → 자율 mode (가장 신뢰)
2. **환경변수 unset 시** 보조 키워드 매칭 — **동사구만**:
   - "ralph 로 돌려", "web-loop 진입", "autonomous-loop 시작"
   - "harness 개선 루프 시작", "자가개선 cycle 진입", "끝까지 끝내줘"
   - **명사 단독** ("자가개선" 만, "ralph" 만) 매칭 금지 — 일반 prompt false positive 차단
```

동사구 목록 (L62–63):
- "ralph 로 돌려"
- "web-loop 진입"
- "autonomous-loop 시작"
- "harness 개선 루프 시작"
- "자가개선 cycle 진입"
- "끝까지 끝내줘"

환경변수: `KZK_AUTONOMOUS=1` (L60)

#### kzk-regression-memory/SKILL.md — L91–105 (§자가-skip guard)

```
자율실행 cycle 의 메인 prompt 면 inject 안 함:

- **환경변수** `KZK_HARNESS_SELF_IMPROVEMENT=1` → 즉시 skip (가장 신뢰)
- **환경변수** `KZK_AUTONOMOUS=1` → 즉시 skip (spec rev6 §자율 mode 판별 #1 우선순위와 통일)
- user prompt 에서 **self-improvement 동사구** grep — 매칭되면 skip:
  - `harness 개선 루프 시작`
  - `스킬 개선해줘`
  - `harness loop 진입`
  - `자가개선 cycle 진입`
  - `자가개선 돌려줘`
  - `메타 cycle 진입`
  - `ralph 로 돌려`
- **명사 단독 금지** (`자가개선`, `메타 cycle`, `ralph`) — 일반 prompt false positive 차단
```

동사구 목록 (L98–104):
- "harness 개선 루프 시작"
- "스킬 개선해줘"
- "harness loop 진입"
- "자가개선 cycle 진입"
- "자가개선 돌려줘"
- "메타 cycle 진입"
- "ralph 로 돌려"

환경변수: `KZK_HARNESS_SELF_IMPROVEMENT=1` (L95), `KZK_AUTONOMOUS=1` (L96)

#### kzk-autonomous-boundary/SKILL.md — frontmatter description (L4)

```
description: "Autonomous-mode boundary — make sure to use this skill whenever the user says 'ralph로 돌려', '끝까지 끝내줘', '자율실행', or any phrasing that requests autonomous multi-commit execution..."
```

동사구/명사구 (frontmatter, L4):
- "ralph로 돌려" ← **주의: 띄어쓰기 없음** ("ralph로")
- "끝까지 끝내줘"
- "자율실행" (명사형 — 단독 매칭 허용 여부 불명)

§Triggers 섹션: cycle 40 에서 제거됨 (본문 없음). 추가 동사구는 Branch contract 섹션 L21 에:
- "ralph", "ulw", "autopilot", "web-loop", harness self-improvement, "끝까지 끝내줘", "자는 동안 진행해", "실행해놔야 queue 보지"

---

### 1B. Canonical 동사구 list (union + drift 정리)

#### Drift 발견 — 띄어쓰기 불일치

| 형태 | 출처 |
|---|---|
| "ralph 로 돌려" (공백 O) | kzk-test-coverage L62, kzk-regression-memory L104 |
| "ralph로 돌려" (공백 X) | kzk-autonomous-boundary frontmatter L4 |

**결정: "ralph로 돌려" (공백 없음) 채택.** 근거: kzk-autonomous-boundary frontmatter 가 사용자 prompt 의 가장 가까운 trigger definition 이고, 한국어 조사 결합 "로" 는 붙여쓰기가 표준 (이 채택이 canonical form 이 됨).

#### Drift 발견 — 동사구 집합 불일치

kzk-test-coverage 에만 있는 구: "web-loop 진입", "autonomous-loop 시작"
kzk-regression-memory 에만 있는 구: "스킬 개선해줘", "harness loop 진입", "자가개선 돌려줘", "메타 cycle 진입"
kzk-autonomous-boundary 에만 있는 구: "자율실행", "ulw", "autopilot", "자는 동안 진행해", "실행해놔야 queue 보지"
공통 구: "ralph로/ralph 로 돌려" (띄어쓰기 drift), "끝까지 끝내줘", "harness 개선 루프 시작", "자가개선 cycle 진입"

#### Canonical list (§33 기준)

**Category A — 명시 trigger 동사구** (사용자 prompt → autonomous mode 진입 의도):

1. "ralph로 돌려"
2. "끝까지 끝내줘"
3. "자는 동안 진행해"
4. "자율실행해줘" ← "자율실행" 명사 단독 금지이므로 동사구 형태만 허용
5. "자율로 돌려"
6. "web-loop 진입"
7. "autonomous-loop 시작"
8. "실행해놔야 queue 보지"

**Category B — 자가-skip 동사구** (자가개선 cycle 메인 prompt → recall hook inject 차단):

1. "harness 개선 루프 시작"
2. "harness loop 진입"
3. "스킬 개선해줘"
4. "자가개선 cycle 진입"
5. "자가개선 돌려줘"
6. "메타 cycle 진입"

**Category C — 환경변수** (최우선, 양방향):

1. `KZK_AUTONOMOUS=1` — Category A 진입
2. `KZK_HARNESS_SELF_IMPROVEMENT=1` — Category B 진입 (regression-memory skip 전용)

**명사 단독 금지** (false positive 차단):
- "자가개선" 단독, "ralph" 단독, "메타 cycle" 단독, "자율" 단독, "autopilot" 단독

---

### 1C. harness-share.md §33 본문 sketch (full)

삽입 위치: harness-share.md L1211 (§32 §7 Cross-ref 끝, 파일 EOF) 직후 — 즉 EOF append.

```markdown

---

## §33 Autonomous-mode Detection SoT

> Single source of truth for all autonomous-mode detection logic. All skills that gate on
> autonomous mode (kzk-test-coverage, kzk-regression-memory, kzk-autonomous-boundary, etc.)
> MUST cross-ref this section. Do not maintain a local keyword list — drift risk.

### 우선순위 (높음 → 낮음)

#### 1. 환경변수 (가장 신뢰 — hook + subagent 양쪽 공통)

| 변수 | 값 | 의미 |
|---|---|---|
| `KZK_AUTONOMOUS` | `1` | 자율 실행 mode 진입 (Category A + B 모두 skip) |
| `KZK_HARNESS_SELF_IMPROVEMENT` | `1` | 자가개선 cycle 전용 진입 (Category B skip — regression recall hook 차단) |

환경변수가 set 된 경우 아래 동사구 grep 생략. 환경변수 우선.

#### 2. Category A — 명시 trigger 동사구 (사용자 → 자율 mode 진입 선언)

user prompt 에서 아래 동사구 중 하나 이상 매칭 시 → 자율 mode 진입 판단.

1. `ralph로 돌려`
2. `끝까지 끝내줘`
3. `자는 동안 진행해`
4. `자율실행해줘`
5. `자율로 돌려`
6. `web-loop 진입`
7. `autonomous-loop 시작`
8. `실행해놔야 queue 보지`

#### 3. Category B — 자가-skip 동사구 (자가개선 cycle → recall hook inject 차단)

user prompt 에서 아래 동사구 중 하나 이상 매칭 시 → self-improvement cycle 판단.
regression-memory 의 UserPromptSubmit hook 이 inject 를 skip.

1. `harness 개선 루프 시작`
2. `harness loop 진입`
3. `스킬 개선해줘`
4. `자가개선 cycle 진입`
5. `자가개선 돌려줘`
6. `메타 cycle 진입`

### 명사 단독 매칭 금지

아래 단독 명사는 false positive 차단을 위해 매칭 금지:

- `자가개선` 단독
- `ralph` 단독
- `메타 cycle` 단독
- `자율` 단독
- `autopilot` 단독

동사구 + 목적격 결합 시에만 매칭. "ralph로 돌려" OK, "ralph" 단독 X.

### 사용처 (cross-ref 의무)

| Skill | 섹션 | 적용 Category |
|---|---|---|
| kzk-test-coverage | §Anti-pattern §자율 mode 메인 직접 TDD 금지 (자율 mode 판별) | A + C |
| kzk-regression-memory | §자가-skip guard | B + C |
| kzk-autonomous-boundary | frontmatter description + §Branch contract ASK FIRST | A + C |

### Cross-ref

- **kzk-test-coverage §자율 mode 메인 직접 TDD 금지**: Category A + C 매칭 시 메인 직접 TDD red 차단
- **kzk-regression-memory §자가-skip guard**: Category B + C 매칭 시 hook inject skip
- **kzk-autonomous-boundary §Branch contract ASK FIRST**: Category A + C 매칭 시 ASK FIRST 의무 진입
```

총 sketch LoC: 약 65 lines (harness-share.md 추가분).

---

### 1D. 3 SKILL update — line range + cross-ref 형식

#### D1. kzk-test-coverage/SKILL.md — L59–64 교체

현재 (L59–64, 6 lines):
```
**자율 mode 판별** (spec rev6 wording 그대로 — `=0 override` 없음):
1. 환경변수 `KZK_AUTONOMOUS=1` → 자율 mode (가장 신뢰)
2. **환경변수 unset 시** 보조 키워드 매칭 — **동사구만**:
   - "ralph 로 돌려", "web-loop 진입", "autonomous-loop 시작"
   - "harness 개선 루프 시작", "자가개선 cycle 진입", "끝까지 끝내줘"
   - **명사 단독** ("자가개선" 만, "ralph" 만) 매칭 금지 — 일반 prompt false positive 차단
```

교체 후 (3 lines):
```
**자율 mode 판별**:
> See harness-share.md §33 Autonomous-mode Detection SoT (Category A + C).
> TDD 금지 적용 범위: Category A 동사구 또는 `KZK_AUTONOMOUS=1` 매칭 시.
```

delta: -3 LoC (6→3)

#### D2. kzk-regression-memory/SKILL.md — L91–105 교체

현재 (L91–105, 15 lines):
```
## 자가-skip guard (codex #5 답 — 동사구만)

자율실행 cycle 의 메인 prompt 면 inject 안 함:

- **환경변수** `KZK_HARNESS_SELF_IMPROVEMENT=1` → 즉시 skip (가장 신뢰)
- **환경변수** `KZK_AUTONOMOUS=1` → 즉시 skip (spec rev6 §자율 mode 판별 #1 우선순위와 통일)
- user prompt 에서 **self-improvement 동사구** grep — 매칭되면 skip:
  - `harness 개선 루프 시작`
  - `스킬 개선해줘`
  - `harness loop 진입`
  - `자가개선 cycle 진입`
  - `자가개선 돌려줘`
  - `메타 cycle 진입`
  - `ralph 로 돌려`
- **명사 단독 금지** (`자가개선`, `메타 cycle`, `ralph`) — 일반 prompt false positive 차단
```

교체 후 (5 lines):
```
## 자가-skip guard

자율실행 cycle 의 메인 prompt 면 inject 안 함.

> See harness-share.md §33 Autonomous-mode Detection SoT (Category B + C).
> 본 hook: Category B 동사구 또는 `KZK_HARNESS_SELF_IMPROVEMENT=1` / `KZK_AUTONOMOUS=1` 매칭 시 즉시 skip.
```

delta: -10 LoC (15→5)

#### D3. kzk-autonomous-boundary/SKILL.md — frontmatter description 내 trigger 동사구 → cross-ref

현재 (L4 frontmatter description, inline list):
```
"...make sure to use this skill whenever the user says 'ralph로 돌려', '끝까지 끝내줘', '자율실행', or any phrasing that requests autonomous multi-commit execution..."
```

교체 후:
```
"...make sure to use this skill whenever the user's prompt matches the Category A trigger phrases in harness-share.md §33 Autonomous-mode Detection SoT (canonical: 'ralph로 돌려', '끝까지 끝내줘', etc.)..."
```

delta: ~0 LoC (inline 치환, 1 line)

---

### 1E. user-queue Q-AUTONOMOUS-SOT RESOLVED update

현재 (docs/harness/user-queue.md L7):
```
- Q-AUTONOMOUS-SOT (2026-05-06 cycle 43 audit) — autonomous-mode detection 동사구 list 가 kzk-test-coverage / kzk-regression-memory / kzk-autonomous-boundary 3 SKILL 분산. drift risk. 권고: harness-share.md §autonomous-mode-detection SoT 신설 + 3 SKILL cross-ref. status: PENDING — 사용자 architecture 결정 필요. resolution: cycle 44+ 후속.
```

교체 후:
```
- Q-AUTONOMOUS-SOT (2026-05-06 cycle 43 audit) — status: RESOLVED (cycle 44). harness-share.md §33 신설 (Autonomous-mode Detection SoT). kzk-test-coverage L59-64, kzk-regression-memory L91-105, kzk-autonomous-boundary frontmatter → §33 cross-ref. canonical form "ralph로 돌려" (붙여쓰기) 채택.
```

---

## Task 2 — Batch D

### 2A. kzk-autonomous-boundary §Branch policy detail

audit 원문 인용 (docs/harness/surveys/2026-05-06-skill-self-application-audit.md L505):
> **kzk-autonomous-boundary §Branch policy detail** (L80-88): compress 9 lines → 4 lines + "see CLAUDE.md Autonomous Execution Boundary".

실제 SKILL.md 현재 내용 (L76–84):
```
## Branch policy detail

- The branch contract is per-session, not hardcoded. Common shapes:
  - PR-flow on `feature/<topic>` (most projects' default)
  - PR-flow on a repo-specific name (`harness-test` for kzk-harness's own self-test convention)
  - Direct-main, no PR (small docs / config sweeps the user explicitly authorized — kzk-harness self-improvement runs in this mode when the user says so)
  - Direct on a non-main feature branch, no PR (long-lived experiment branch)
- Under PR-flow: **One Plan = one PR (no batch).** First plan-direction error → other plans stay protected. PR description must include `CLAUDE.md updated to match current state` + `deepinit ran` lines (see `kzk-pre-merge-sync`).
- Under direct-main / direct-no-PR flow: same atomic-commit discipline applies. `kzk-pre-merge-sync` checks run before user-visible milestone commits (see that skill).
```

압축안 (4 lines):
```
## Branch policy detail

> See CLAUDE.md "Autonomous Execution Boundary" for branch shape examples (PR-flow / direct-main / direct-no-PR / long-lived branch).

- Under PR-flow: **One Plan = one PR (no batch).** PR description must include `CLAUDE.md updated` + `deepinit ran` (see `kzk-pre-merge-sync`).
- Under direct-main flow: atomic-commit discipline applies; `kzk-pre-merge-sync` checks before milestone commits.
```

delta: -5 LoC (9→4, 섹션 header 포함)

### 2B. kzk-pre-commit-gate §Doc-only 통합

audit 원문 인용 (L506):
> **kzk-pre-commit-gate §Doc-only commit exception + §Doc-only patch policy** (L182-212): merge two sections, eliminate split → save ~6 lines.

실제 현재 내용:
- §Doc-only commit exception: L149–165 (17 lines)
- §Doc-only patch policy: L167–180 (14 lines)
- 두 섹션 사이 빈 줄 포함 총 범위: L149–180 (32 lines)
- 공통 조건: staged diff 가 doc 파일만, source file 없음
- 차이: exception 은 Gate skip 목록, policy 는 minimal gate set 명시

통합안 (26 lines, -6 LoC):
```
## Doc-only commit exception and patch policy

If the commit touches **no** source code — only docs/configs (`*.md`, `docs/**`, `CLAUDE.md`, `DESIGN.md`, `harness-flow-progress.md`, `skills/**/*.md`, `.claude/skills/**/*.md`, `docs/screenshots/**`) — then apply the minimal gate set:

- Gate 0 (AGENTS.md sync) N/A unless the doc commit itself adds/removes files under a source root
- Gate 1 (ai-slop-cleaner) only on touched md if needed
- Gate 1.5 (secrets scan) **always required**
- Gate 2 (build) skipped
- Gate 3 (test) skipped
- Gate 4 N/A
- Gate 5 N/A
- Verify: `bash install/verify-install.sh --ac 2` (kzk marker block row count, ≤ 5s)

Doc-only commits go straight to commit after Gate 1.5 + AC2.

Any single source-code line in the same commit revokes this exception → run all applicable gates.

**AGENTS.md / README.md**: standalone update (no source add/delete) = doc-only OK. Same commit as source add/delete = exception revoked.

**Skill files** (`skills/**/*.md`): doc-only ONLY for existing skill edits. ADDING a new skill triggers Gate 0 (if AGENTS.md hierarchy present) + README/CLAUDE.md skill-count update flow.

**Autonomous mode**: commit without user prompt when gates pass. Non-autonomous: confirm with user.
```

delta: -6 LoC (32→26)

### 2C. kzk-autonomous-boundary halt-table Action cell 압축

audit summary table (L422):
> L59-64 halt table Action cells — 압축 권고

실제 SKILL.md L55–65 halt-table Action cell 확인:
- Q-TDD-MAIN Action (L59): `halt + user-queue entry \`Q-TDD-MAIN — 자율 cycle 의 메인 직접 TDD 시도, fresh sonnet dispatch 재시작 필요\`. 메인 직접 test 작성 즉시 중단. cross-ref: \`kzk-test-coverage\` §Anti-pattern §자율 mode 메인 직접 TDD 금지 / \`kzk-large-task-delegation\` §Anti-self-verification boilerplate`
- Q-MAIN-DIRECT-EDIT Action (L60): similarly verbose
- Q-VERIFIER-FAIL Action (L61): similarly verbose

압축 방향: cross-ref 부분 → 각 Q-entry 의 Action cell 에서 별도 cross-ref column 으로 분리. 단 audit 는 "compress Action cells" 라 했으나 구체 line range 미명시 (L59-64 는 halt-table 전체 행 범위). executor 가 판단할 범위:

Action cell 압축 예시 (Q-TDD-MAIN):
- 현재: `halt + user-queue entry \`Q-TDD-MAIN — 자율 cycle 의 메인 직접 TDD 시도, fresh sonnet dispatch 재시작 필요\`. 메인 직접 test 작성 즉시 중단. cross-ref: kzk-test-coverage §Anti-pattern §자율 mode 메인 직접 TDD 금지 / kzk-large-task-delegation §Anti-self-verification boilerplate`
- 압축 후: `halt + Q-TDD-MAIN entry. cross-ref: kzk-test-coverage §자율 mode 메인 직접 TDD 금지`

delta: 약 -8 LoC (5행 × 평균 1.6 line 절감)

---

## Acceptance criteria for executor

1. harness-share.md L1212 (EOF) 직후 §33 block 삽입 확인 — `## §33 Autonomous-mode Detection SoT` 헤더 존재
2. §33 Category A 동사구 8개 전부 존재, "ralph로 돌려" (공백 없음) 형태
3. §33 Category B 동사구 6개 전부 존재
4. §33 환경변수 표 2행 (`KZK_AUTONOMOUS`, `KZK_HARNESS_SELF_IMPROVEMENT`) 존재
5. kzk-test-coverage L59–64 → 3-line cross-ref (`See harness-share.md §33`) 로 교체 완료
6. kzk-regression-memory L91–105 → 5-line cross-ref 로 교체 완료
7. kzk-autonomous-boundary frontmatter description → §33 언급 포함 수정 완료
8. user-queue.md Q-AUTONOMOUS-SOT → status: RESOLVED (cycle 44) 로 업데이트
9. kzk-autonomous-boundary L76–84 §Branch policy detail → 4-line 압축안 완료
10. kzk-pre-commit-gate L149–180 §Doc-only 두 섹션 → 단일 통합 섹션 완료 (-6 LoC)
11. 각 변경 SKILL version bump: kzk-test-coverage 1.5.0→1.6.0, kzk-regression-memory 1.3.0→1.4.0, kzk-autonomous-boundary 1.6.0→1.7.0, kzk-pre-commit-gate (현재 버전+0.1)
12. harness-share.md 파일 line count 증가 확인 (≈+65 lines)

---

## LoC delta 추정

| 항목 | before | after | delta |
|---|---|---|---|
| harness-share.md §33 신설 | 0 | +65 | +65 |
| kzk-test-coverage L59-64 교체 | 6 | 3 | -3 |
| kzk-regression-memory L91-105 교체 | 15 | 5 | -10 |
| kzk-autonomous-boundary frontmatter | 1 | 1 | 0 |
| kzk-autonomous-boundary §Branch policy | 9 | 4 | -5 |
| kzk-autonomous-boundary halt-table cells | ~16 | ~8 | -8 |
| kzk-pre-commit-gate §Doc-only 통합 | 32 | 26 | -6 |
| user-queue.md 1 line update | 1 | 1 | 0 |
| **합산** | | | **+33 LoC** |

Note: §33 신설이 net positive. 전체 harness 압축 목표 관점에서는 Task 1 이 SoT 신설 비용, Task 2 가 압축 이익.

---

## frontmatter version bump list

| Skill | 현재 | 변경 후 |
|---|---|---|
| kzk-test-coverage | 1.5.0 | 1.6.0 |
| kzk-regression-memory | 1.3.0 | 1.4.0 |
| kzk-autonomous-boundary | 1.6.0 | 1.7.0 |
| kzk-pre-commit-gate | (확인 필요) | +0.1.0 |
| harness-share.md | (version 없음 — 파일 내 버전 헤더 없음) | N/A |

---

## 파일 편집 순서 (executor 권장)

1. harness-share.md — §33 EOF append (가장 먼저: 3 SKILL cross-ref 의 target 이므로)
2. kzk-test-coverage/SKILL.md — L59-64 교체 + version bump
3. kzk-regression-memory/SKILL.md — L91-105 교체 + version bump
4. kzk-autonomous-boundary/SKILL.md — frontmatter + §Branch policy detail + halt-table + version bump
5. kzk-pre-commit-gate/SKILL.md — §Doc-only 통합 + version bump
6. docs/harness/user-queue.md — Q-AUTONOMOUS-SOT RESOLVED update
