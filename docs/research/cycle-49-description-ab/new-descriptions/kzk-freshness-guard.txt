Stale meta-doc detection via CRG symbol reverseRefs + auto-fix per doc type (AGENTS row, CLAUDE section, spec line-ref). Runs at Gate 0.5, spec-and-review Step 0, pre-merge §4. Triggers: 'stale 체크', 'freshness guard', 'Gate 0.5', 'KZK_GATE05_SKIP'. References harness-share.md §30.
